# AI Context

**Updated:** 2026-09-01 10:12 (America/New_York)
**Last run:** Phase 2B / **Session 2B.8 — both ProspectHub directory tables adopt the generalised column-layout machinery.** Compartment UI.
**State:** `node --check app.js` clean, run against the file **on disk after the commit** · `check_ids.py` at its standing baseline of two (`{'export-backup-btn', 'restore-backup-input'}`), before and after, on disk · `CACHE_NAME` **v104 → v106**, CONFIRMED LIVE (`caches.keys()` `['vantageprm-cache-v106']`, `controlled: true`) · `app.js` 14,663 → **14,883** / `index.html` 3,396 → **3,399** / `style.css` 4,322 → **4,500** · **console CLEAN — 5 lines, zero red** on the second of two consecutive boots · `localStorage` 27,840 → **28,055 bytes**, and the +215 is exactly the two new `columnLayouts` keys · **git state UNKNOWN — no `device_bash` on this machine**, third session running · deployed n/a
**Estimate vs actual:** sized **M / ~10 min / High**; ran **M**, no strikes, no rework beyond one measured retune of the column widths. **Michael's time was ~0 minutes** — the plan said "inputs needed: none" and that held; he has judgement calls waiting but nothing blocked. Fourth consecutive session confirming 2B.4's lesson that the automation browser being up dominates sizing.
**One-glance version tell:** open ProspectHub and drag the divider between two column headers on either directory table. **The column resizes, the width survives a reload, and dragging a header sideways reorders it.** Second tell: `Object.keys(state.columnLayouts)` returns `["taskhub","prospects","companies"]`.

## What was done

Registration, not invention. **No resolver, writer or drag function was modified** — which is the outcome Session 2B.2 existed to buy, and it held exactly.

- **`PROSPECTS_COLUMNS` (7) and `COMPANIES_COLUMNS` (5), with a real default width on every column**, declared beside `TASKHUB_COLUMNS` because `COLUMN_TABLES` is a `const` that reads all three at module-evaluation time — a list declared further down the file is in its TDZ and the registry throws before boot.
- **Two `COLUMN_TABLES` entries** (`prospects` / `companies`), and that is the whole registration. `ensureStateDefaults()` already loops the registry, so both records seeded themselves with C15's `0` ("unset — use the code default") and no migration was written.
- **`renderLayoutHeaderRow(tableId)`** — one new helper. It rebuilds a registered table's header `<tr>` from the resolver and **returns the resolved column list**, so the caller's body loop uses the same array; correct data under a wrong label is structurally impossible. **It never replaces the `<thead>` element**, only its `<tr>`. TaskHub deliberately does NOT use it (checkbox th, sort arrows, per-th click handler, the C16-row-3 check — four TaskHub-only concerns).
- **Both row builders rewritten** from `innerHTML` template strings to a per-column `switch`, emitting cells in `layoutColumns()` order, plus `data-prospect-id` / `data-company-id` (the registry's `rowSelector`) and a trailing spacer `<td>`.
- **The empty-state colspans are now DERIVED, and one of them was wrong.** Both literals on the contacts table read `5` against a **seven**-column table — stale since City/State/Metro were added. Now `cols.length + 1`.
- **`index.html`:** the two static `<thead>` blocks became `<thead id="prospects-thead" class="prospects-table-head"></thead>` and the companies twin. **The hand-written `<th>`s are gone on purpose and must not be restored.**
- **`style.css`:** a new `✅ PROSPECTHUB DIRECTORY TABLES` block after the TaskHub one — purple 18% header tint (dark `#241b38` / light `#c2b7d7`), sticky `th`, `table-layout: fixed` **on the two ids only, never `.premium-table`**, dividers, resize/drag states, and `#view-prospects .table-scroll-container { padding-top: 0; overflow-x: auto }`. Every selector leads with an id or the view id. **The 🧱 HUB SHELL block is still last in the file.**
- **Task 6 of the plan was a no-op:** 2B.2 renamed rather than delegating, so there were no `taskHub*` delegates to remove. Grep for all six old names returns zero.

## Verified — real output, not a claim

Ran on Michael's Chrome via the extension, against his live `localhost:5000` database at his real 1269×1090 window.

| Check | Result |
| --- | --- |
| Registry live after boot | `layoutDragInit` `{"taskhub":true,"prospects":true,"companies":true}`; both records auto-seeded with all-`0` widths |
| **Resize, prospects** | Title `165 → 237`. Record `{"title":237, rest 0}`, **persisted to `localStorage`**, every neighbour unchanged |
| **Resize, companies — the LEFT half of the straddle** | Grabbed Location's left edge; **Industry** resized `165 → 113` (the 1.8 amendment works on the new tables). Location 190 / Website 105 / Tags 165 **unchanged**; the spacer absorbed it, `10 → 62` |
| **Reorder, prospects, TWO columns in a two-frame gesture** | `[name,company,title,city,state,metro,tags]` → `[name,company,title,**metro,city,state**,tags]`. Row 0 Metro cell reads `"San Francisco, CA"` — data followed the label. Cell counts 8/8/8/8, header 8 |
| **Reorder, companies, THREE columns in one gesture** | `[name,industry,location,website,tags]` → `[name,**tags**,industry,location,website]`. Row 0: `Stripe`, `<SPAN>`, `Fintech`, `San Francisco, CA`, `<A>` — the real badge and link cells moved with their column |
| **Reload persistence** | Both records identical after reload; headers re-rendered at `title:237` and `industry:113` in the saved order |
| **TaskHub regression — resize** | `dueDate 339 → 281`, persisted |
| **TaskHub regression — reorder** | `[dueDate,title,…]` → `[title,dueDate,…]`, persisted; row 0 `Task Title = "Confirm AV requirements"`, `Due Date = "2026-09-09"` |
| **TaskHub regression — C16 row 3** | Sort **unchanged** after each drag's own trailing click; then three consecutive clicks read `desc` → `asc` → `desc`. `layoutSuppressNextHeaderClick` clear afterwards |
| **TaskHub regression — reload** | Record survived; `prospects` and `companies` records untouched by every TaskHub gesture, and vice versa |
| **ZIP round trip, all three keys** | Exported through the real `exportZIPBackup()` (22,163 bytes, also written to the backup folder). **Poisoned `state.columnLayouts` to `{}`**, drove the real `processRestoreFile()` with the real blob → came back **character-identical** with all three keys. Counts after: 4 prospects / 5 companies / 31 tasks / 1 audience / 30 media; `dateMode "business"` |
| **Both headers stuck SIMULTANEOUSLY** | 30 synthetic rows injected into each `<tbody>` (never into `state`). Both wrappers scrolled to `scrollTop 180`, both `scrollHeight > clientHeight`, `#canvas-body` scrolling **neither** way. **Screenshot + a zoom of the header edge in chat — no sliver of a row above either header** |
| **Truncation, not stretching** | Title dragged `237 → 67`, below its ~77px label floor. Header renders `"TIT…"`, cells `"De…" / "He…" / "Dir…" / "VP …"`; **every neighbour unchanged**, the 170px went to the trailing spacer (`0 → 120`). **Screenshot in chat** |
| No horizontal scroll at the defaults | `scrollWidth === clientWidth` on both wrappers; spacers 22px and 10px |
| Console, second of two consecutive boots | **5 lines, zero red** |
| All six hubs after the final reload | Every panel `active-panel` at a real height (903–923px); `#prospect-inspector` still `null` |
| `check_ids.py` / `node --check` | Both run against the files **on disk after the commit**; baseline pair, clean parse |
| Cleanup | Layout records restored to the seeded state (Michael's TaskHub record verbatim; the two new keys at code defaults so he judges the widths I chose, not the ones I dragged). `forceShowAllContacts: true` / `forceShowAllCompanies: false` restored, search boxes cleared, `taskSort` back to `dueDate asc`, parked on the dashboard. **Zero `[data-synthetic="1"]` nodes left** |

## Assumptions logged this session

1. **The ZIP round trip POISONED `state.columnLayouts` instead of calling `wipeAllData()`.** The Done-when asks for a round trip; it does **not** ask for a wipe, and the phase's Backup-points list deliberately grants 2B.8 no backup point — which is the plan saying this session must not wipe. Poisoning exactly the store under test makes the assertion able to fail (the `taskSettings` lesson) while every other store is rewritten by the restore with identical values. The real `exportZIPBackup()` and the real `processRestoreFile()` router both ran unmodified.
2. **The column-default widths were retuned once, mid-session, after measuring.** The first pass summed to 898/880 against a wrapper of 898.67 — and overflowed, because `.table-scroll-container` keeps **12px of padding on each SIDE** (only its `padding-top` is removed). Real budget is ~855px, not ~899. Now 853 and 845. `CACHE_NAME` was bumped twice (v105, then v106) as a result.
3. **`renderLayoutHeaderRow()` serves the two plain tables and NOT TaskHub.** Folding four TaskHub-only concerns into a shared helper to save a dozen lines is how the one working consumer gets broken by a session that never meant to touch it.
4. **The spacer keeps the class name `taskhub-col-spacer`, and the drag still writes `taskhub-col-resizing` / `taskhub-col-dragging` on these tables.** Those classes are written by the *generalised* handler; renaming them is a three-file change with no behavioural payoff. Same call 2B.2 made and recorded for `#taskhub-drop-line`.
5. **`prospects.metro` is keyed to the LABEL, not the field.** The column shows `p.location` and always has. Keying it `location` would have read better and would silently invalidate every saved layout record if the field is ever renamed.

## Backup coverage — DIRECTIVES §4

**No new store of user-writable data was created.** P9 holds: no new persisted field, no `ensureStateDefaults()` entry beyond the registry loop that already existed, no CSV column, no migration.

**One existing store gained two keys.** `state.columnLayouts` was built keyed by table id precisely so a second table adopts it; its entire backup coverage is the single `["Column Layouts", JSON.stringify(...)]` row in `prm_settings.csv` (C17), and `wipeAllData()` already clears the whole object. **New keys therefore needed zero export or restore work — and that claim was proved, not assumed:** poisoned to `{}` and restored character-identical with all three keys through a real ZIP. **`state.columnLayouts` is COVERED.**

`state.prospects` and `state.companies` were read only. **Covered, untouched.**

## Open items

- **⚠️ NEEDS YOUR EYES — THE TWELVE DEFAULT WIDTHS, THE PURPLE, AND THE 6px RESIZE ZONE ON THESE TWO TABLES.** All three were on the plan's list and none can be judged from a number. The widths are code defaults: drag any edge and yours wins forever after.
- **⚠️ THE SNAPSHOT FOLDER PERMISSION HAS LAPSED — the sidebar reads "Not protected" (red).** Pre-existing, not caused by this session (the File System Access handle needs re-granting after some reloads, which DIRECTIVES §0 says must surface visibly — it is surfacing correctly). **Re-grant it next time you are in the app**; there is no automatic snapshot until you do.
- **✅ THE ZERO-BYTE SNAPSHOT QUESTION IS SETTLED — THE PRUNER IS NOT LYING.** 2B.6 flagged two readings and 2B.7 could not choose between them. This session caught it: boot at 10:11:32 logged `Ignoring 1 zero-byte snapshot file(s)` + `Pruned 1 file(s) — 1 zero-byte, 0 aged out. 13 kept.`, and the **very next boot nine seconds later logged NEITHER line**. The pruner really deletes the file it reports deleting. The residual question is only why a truncated write happens at all — occasional, benign, **off the open list**.
- **⚠️ THE REVIEW PASS IS STILL OWED.** 2B.6 was the review point and the full day-of-outreach pass has not happened. Findings 1 and 2 are banked below and take 2B.11+.
- **✅ REVIEW FINDING 1 — the Company tab collapses by default, expandable, disclosure arrow left of the company name.** Michael, 2026-09-01. Scope is `renderDetailCompany()` and its `.pd-company-card` / `.pd-company-grid` styling only. **Do not invent a store for the expanded/collapsed state — default to NOT persisting** (P4's rule for tab and scroll position); if it must be remembered it joins `state.columnLayouts` under its own key, never a new top-level store. **2B.11+.**
- **✅ REVIEW FINDING 2 — "carry me back to the contact I came from." TWO ASKS, VERY DIFFERENT COSTS.** **2a (SMALL, no routing):** `detailProspectId` is cleared in exactly one place — `closeProspectDetail()` — so a chip navigation leaves it set; the change is a branch at nav-tab target `"prospects"` when it is still set. **2b (the mouse back button) IS A SCOPE AMENDMENT** — it needs `history.pushState`, which the scope bans. A **sentinel** design (`pushState({vantageDepth:n}, "", location.href)`) satisfies P9's letter completely, so the P9 objection is answerable and the scope ban is the live obstacle. Read `DECISIONS.md` before proposing the reversal. **2B.11+.**
- **⚠️ 2B.7's duplicate-email warning wording still needs your eyes on screen.** Carried unanswered.
- **⚠️ THE IDENTITY BLOCK BREAKS CONTRACT S1 BELOW 1150px CANVAS WIDTH — pre-existing, 2B.11's ground.** Not re-measured this session; the window was 1269px throughout, above the breakpoint.
- **⚠️ Carried, unanswered from 2B.4 / 2B.5:** (b) the `.pd-add-block` label "ADD THIS PROSPECT TO AN AUDIENCE". (c) the tab strip's weight against the identity block; whether Interactions is the right default landing tab; the redundant "Tasks" heading inside the Tasks tab; the non-sticky interactions table header.
- **Phase backlog, unchanged:** the headquarters duplication question needs the PRODUCTION database loaded (the importer is exonerated — fallback chains, never a join); the ZIP restore reports **1 orphaned task** on a round trip of an untouched database, a data condition predating this phase (seen again this session).
- **⚠️ 2B.9 DEPENDS ON THIS SESSION AND ITS TARGET IS ONE ROW ABOVE THE TABLES I JUST TOUCHED.** `#prospect-tag-chooser` still carries its three inline `onfocus`/`onblur`/`onchange` handlers at `index.html` ~252. Nothing here disturbed it.
- **Carried, unchanged:** `#btn-see-all-contacts` does not exist (`btn-show-all-contacts` / `btn-show-all-companies`). **Both `forceShowAll*` true blanks the directory — CONFIRMED AGAIN this session, both `.table-section`s go `display: none`.** `#companies-datalist` is dead markup. `pros-sarah` carries no `conference*` keys. The four always-on conference boxes read as grey ghosts. CampaignHub identifies itself twice. Dashboard/DataHub emptiness is `renderDashboardView()`'s `slice(0, 5)`. MediaHub's tag rail off the right edge. `.checkbox-scroller` inline `max-height: 350px`. `.tags-filter-scroller` `max-height: 400px`. **`.gitignore` snapshot-glob gap — one line, `vantage_snapshot*`, still NOT fixed.**
- **GIT STATE IS UNKNOWN.** No `device_bash` again — the bridge staged and committed files but could not execute anything on the machine. Nothing here claims what has been committed.
- **Unchanged from Phase 1:** two Vantage windows overwrite each other; `parseCSVRow()` `""` gap; repo is PUBLIC; DIRECTIVES §0 compliance undecided; stale `..\backups\`; `schema_update.sql` still deletable.

## Files changed

`app.js` (14,663 → **14,883**), `index.html` (3,396 → **3,399**), `style.css` (4,322 → **4,500**), `sw.js` (**v104 → v106**), `ai/BUILD_NOTES.md`, `ai/AIContext.md`, `ai/archive/2026-09-01_1012_AIContext.md` (new).

**No `DECLARATIONS.md` or `DECISIONS.md` change.** 2B.10 still owes the seventh-view-panel amendment and the line-count reconciliation; "six hubs" stays true and must not be edited to say seven.

## Next step

**Session 2B.9 — `#prospect-tag-chooser` replaced with the Advanced Query picker.** Compartment UI, size M, ~6 min, depends on this session. Reuse the six `AQ_PICKERS` functions; write no seventh. It must also remove the three inline handlers in `index.html` — the last ones on that control — and route every read of the selection through one accessor (P8: the id is the contract, the widget is not).

**But the review pass is still owed, and the plan is explicit that it produces sessions.** Do the day-of-outreach pass before 2B.9 if you want 2B.11+ numbered sensibly.

**No ZIP backup point before 2B.9** — it touches no write path. The next mandatory one is before 2B.10.

**Carry forward:** the `🧱 HUB SHELL` block must stay LAST in `style.css`. `#canvas-body` is never edited. `state` is not `window.state` (top-level `function` declarations ARE on `window`). One Vantage window at a time. `state.selectedProspectId` and `detailProspectId` are two cursors and **do not converge in this phase**. No routing, ever. `switchView()` calls `saveState()`. Inject the transition kill-switch after every reload. `resize_window` does not work on this machine. **Both query surfaces stay DEFERRED — `renderAqInspectorDrawer()`, the `aq-insp-*` ids, and the Audience Query Engine are not touched.**
