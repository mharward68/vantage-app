# AI Context

**Updated:** 2026-08-30 08:40 (America/New_York) — review-pass amendment appended
**Last run:** Phase 1 / Session 1.10 — Stationary header block, column layout store, and drag-to-resize  **Compartment(s):** UI, with DATA
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two · console **character-identical** to the unmodified v77 tree (both booted the same way and driven through all six hubs, diffed after normalising the port) · `CACHE_NAME` **v79** (v78 shipped, v79 for the review amendment) · deployed n/a
**Estimate vs actual:** planned **L** / ~15 min of Michael's time. Actual **L**, and ~**0 min** of his — nothing escalated, no input needed, no three-strikes, no frozen contract modified. Two defects were found and fixed inside the session by screenshot rather than left for his review pass.

## Done

- **§13.3 + §15.2 — TaskHub is exactly one screen tall and only the list scrolls.** `#view-tasks` is `height:100%; min-height:0`, `.taskhub-panel-card` is `flex:1 1 auto; min-height:0`, `#view-tasks .taskhub-table-wrap` is `flex:1 1 auto; min-height:0; overflow-y:auto; overflow-x:auto`. `#canvas-body` is **untouched**. Verified: with TaskHub active `#canvas-body` scrollHeight === clientHeight (773/773, was 1513/773); the other five hubs' numbers are byte-identical to the unmodified tree. Filter strip viewport top 178 → 178 with the wrapper scrolled to 400; the sticky column header 334 → 334.
- **§15.1 — the bulk bar moved up, in a slot that reserves its height.** `#taskhub-bulk-actions` moved in `index.html` to sit between `#taskhub-range-group` and the Per Page row; **Prev/Next stayed** below the table. Its markup, id, four controls and handlers are unchanged, and `renderTaskHubBulkBar()` / `syncTaskHubSelectionUI()` were not touched. The new wrapper `#taskhub-bulk-slot` turns the bar's own `.hidden` toggle from `display:none` into `visibility:hidden`, so the slot keeps the bar's real height with **no magic number**. Verified: table wrapper top and first-row top are **identical** (333 / 386.5) at zero selected and at one selected. Bulk Mark Complete still works from the new position (3 selected → 3 completed → 3 history entries).
- **§15.3 — the column header takes TaskHub's cyan, opaque, one shared class.** `thead.task-table-head th` on **both** TaskHub's and `#modal-task-orphans`' `<thead>`. Dark `#0c2c31` / `#06b6d4`; light `#b0cad0` / `#0891b2` — literal hexes with a `.light-theme` sibling, no custom property. Verified: computed `background-color` is `rgb(12,44,49)` / `rgb(176,202,208)` with **no alpha channel**, and the two tables' background and color strings are **identical**. Screenshots in both themes, hub and orphan window.
- **§15.4 — TaskHub no longer creates tasks.** `#taskhub-new-btn` and its listener removed in the same edit. `openTaskEditor()`'s `isNew && !prospectId` branch **deleted**; `needsPicker = isOrphan` alone. The empty-state text no longer tells the reader to click a button that is gone. Verified: no control matching `/new task/i` anywhere in `#view-tasks`; the inspector's own "+ New Task" still creates normally via a dispatched click (`pros-005`, saved, picker correctly hidden); `saveTaskFromEditor()` still refuses a prospect-less task with *"Pick a prospect for this task."* and writes nothing.
- **§15.4 orphan preservation — intact.** Two seeded orphans: `taskOrphanCount()` 2, chip "⚠️ 2 Missing Prospect", both listed in the window with their stored ids verbatim. The search still reveals on `isOrphan` after the branch deletion. An orphan opened, edited and saved **with nothing picked kept `pros-DELETED-B` unchanged**. One repaired through the C14 search → count 2 → 1, chip re-read.
- **C15 — `state.columnLayouts`.** Default in `ensureStateDefaults()`, resolver `taskHubLayoutRecord` / `taskHubColumns` / `taskHubColumnWidth` / `setTaskHubColumnWidth`. **Both header AND cells render from the resolver**, so they cannot drift apart. Verified with a hand-edited layout carrying an unknown key (`assignee`) and missing a known one (`lastName`): unknown dropped, missing appended, saved widths honoured, unsaved widths fell back to code defaults, cells followed their headers, **no errors**.
- **C16 — all three hit zones, only resize built.** One delegated `mousedown` on the static `#taskhub-thead`. Verified: cursor `col-resize` 2px from a right edge and `pointer` 30px from a left edge; a +120px drag on Company 220 → 342, persisted once on mouseup and surviving a reload; a plain click still sorts **both directions**; a ≥4px body drag leaves the sort **unchanged**; the very next plain click sorts again (the flag is one-shot); the checkbox column and the trailing spacer carry no `data-col-key` and are neither.
- **C17 — backup coverage.** `["Column Layouts", JSON.stringify(...)]` in `generateSettingsCSV()`, read behind `sawColumnLayouts`, added to `wipeAllData()`. Verified through a **real** ZIP: export → `wipeAllData()` (layout → `{}`) → `processRestoreFile()` → layout **character-identical** to pre-export, and the widths render.
- `CACHE_NAME` bumped **v77 → v78**.

### Two defects found and fixed inside the session, both by screenshot

- **The sticky header was not at the top of the scrollport.** `.table-scroll-container`'s 12px `padding-top` is a strip *above* a `top:0` sticky element, so a 12px sliver of the scrolling row floated over the header. Every computed style said "opaque and stuck", and they were right. Fixed with `padding-top: 0`, which needed the `#view-tasks` prefix because `.table-scroll-container`'s `padding` shorthand is declared ~950 lines later at equal specificity.
- **`position: relative` on `#taskhub-table th` silently un-stuck the header.** Same source-order-at-equal-specificity trap, one rule earlier in my own new block. Removed — C16 hit-tests the pointer's x rather than using an absolutely positioned handle, so nothing needs a positioned ancestor.

### Review pass, same day — §15.1a, the bar is faded at zero, not hidden (v79)

- **Michael's report was "Bulk edit items are missing."** Nothing was broken — the bar was `display:none` at zero selected inside its height-reserved slot, per §15.1 as written. But an empty 50px gap between the filter strip and the Per Page row does not read as a toolbar awaiting a selection; it reads as a component that failed to load. **§15.1's own "reversible if the reserved strip reads as dead space" clause fired within minutes of real use.**
- **Fixed: always visible, faded and inert at zero, lit on the first tick.** The CSS rule on `#taskhub-bulk-slot > #taskhub-bulk-actions.hidden` now means `opacity: 0.45; pointer-events: none` rather than `display: none`. `renderTaskHubBulkBar()` still toggles `.hidden` and that class is still the single source of truth; only what it *means* for this bar changed.
- **`renderTaskHubBulkBar()` also sets `disabled` on the three controls.** `pointer-events: none` stops the mouse and nothing else — without `disabled` they stay tab-reachable and fire on Enter while greyed out. Verified: a real click and a real Enter on the faded Mark Complete changed nothing (21 open before, 21 after), focus refused to land on it, and two rows selected still completed normally (21 → 19).
- **The reserved height is unchanged and was re-verified**: table wrapper top 333 → 333 and first row top 374.5 → 374.5, zero selected vs one. That property is the whole reason §15.1 exists and it was not traded away.
- Scope **§15.1a** written into `ai/spec/taskhub-scope.md`. `CACHE_NAME` **v78 → v79**.
- **Note for the next session: the first report was a false alarm caused by the service worker,** not by this. `sw.js` is cache-first, so the reload that first sees a new `sw.js` is still served the OLD document; the second reload gets the new one. The one-glance version tell for 1.10 is whether "+ New Task" is still in the TaskHub header.

## Files changed

**Modified:** `app.js` (`ensureStateDefaults` C15 default; `generateSettingsCSV` / `restoreSettingsFromCSV` C17 row; `wipeAllData`; `openTaskEditor` picker test + comment block; `TASKHUB_COLUMNS` gains a width per column; new `taskHubLayoutRecord` / `taskHubColumns` / `taskHubColumnWidth` / `setTaskHubColumnWidth` and the C16 constants; `renderTaskHubTable` header and body both build from the resolver, plus the trailing spacer; new `taskHubHeaderCell` / `taskHubInResizeZone` / `initTaskHubHeaderDrag` before the orphan block; `setupEventListeners` loses the `taskhub-new-btn` listener and gains `initTaskHubHeaderDrag()`), `index.html` (`#view-tasks` restructured, `#taskhub-bulk-slot`, `#taskhub-table`, `task-table-head` on both `<thead>`s, `#taskhub-new-btn` removed), `style.css` (`✅ TASKHUB LAYOUT` block), `sw.js` (`CACHE_NAME`).
**Untouched:** `#canvas-body`, the other five hubs, `renderTaskHubBulkBar`, `syncTaskHubSelectionUI`, `makeTableColumnsResizable` and the email-accounts table, every completion and history path.

## Backup coverage (DIRECTIVES §4)

**One new store of user-writable data: `state.columnLayouts`. It IS covered.** Coverage is contract C17's single `["Column Layouts", <json>]` row in `prm_settings.csv` — emitted by `generateSettingsCSV()`, read by `restoreSettingsFromCSV()` behind `sawColumnLayouts`, defaulted in `ensureStateDefaults()`, cleared in `wipeAllData()`. It reaches every path the settings CSV already reaches, including the ZIP and the state JSON in every snapshot. **Proved, not asserted:** a real export → wipe → restore returned it character-identical. Nothing here is uncovered.

## Assumptions made

- **The C15 default record ships `dueDate: 0`, not the `104` in C15's example.** C15's normative rule is "`0` means unset — use the code default"; the `104` in its illustrative code block was Session 1.5's code default copied in. Under `table-layout:fixed` a column can no longer grow, and 104 renders **"DUE DAT…"** — freezing it into every first-run record would ship a truncated header no later code-default fix could reach. The record's shape, keys and semantics are exactly as C15 specifies. The code default is now **124**. Reversible; flagged for Michael.
- **`renderTaskHubTable()` builds header AND cells from the C15 resolver in 1.10**, which is part of Session 1.11's task 3. Doing only the header would create the exact header/cell desync 1.11's risk note warns about, and doing neither would leave `order` written but never read — so the C15 read/fallback rule 1.10 owns would be untestable. **1.11's task 3 is therefore already done**; 1.11 adds the drag branch and nothing else.
- **The trailing spacer column** (a width-less `<th>` plus a `<td>` per row) is how the table stays full-width without one column stretching when another shrinks — the same trick `makeTableColumnsResizable()` uses for the email-accounts table. It is not in C15's `order` or `widths`, and is neither sortable nor resizable. Reversible.
- **Code default widths are 124 / 140 / 160 / 220 / 340.** Chosen so the table fits a 1440px window with a small spacer rather than overflowing horizontally out of the box. Guesses; a real window and a real hand decide.
- **A malformed `Column Layouts` cell is caught and the flag left down**, so a corrupt width cannot take the whole settings restore (every tag, every reachout type) down with it.
- **Verification ran in headless Chromium via Playwright in the cloud sandbox, against a copy of the repo** seeded with 40 scripted contacts, 12 companies and 120 scripted tasks — **not** Michael's production database and not his browser profile. Nothing was written to his data. `index.html` was **not** modified for the harness: the pinned jszip 3.10.1 the page requests from cdnjs was served from disk by a Playwright route, because the sandbox has no egress.

## Open items

- **Needs Michael's eyes — the 5px resize hit zone.** The plan already flags it: 5px is the kind of number only a real hand can judge. Change `TASKHUB_RESIZE_EDGE_PX` in `app.js`; nothing else moves.
- **CLOSED same day — the reserved bulk-bar strip.** It did read as dead space, within minutes. Resolved by §15.1a (faded, not hidden). Do not re-raise, and **do not "tidy" the fade back to `display: none`** — that is the state Michael reported as broken.
- **Needs Michael's eyes — the `dueDate: 0` / code-default-124 assumption above**, and the five default widths.
- **Still open: the repo is PUBLIC**, and DIRECTIVES §0 compliance is still undecided. Git history is permanent; Phase 2's Firebase config and migration exports are the pressure point. Nothing real is exposed today.
- **Still open: the stale `E:` clone.** `E:\01_AppDevelopment\02_Vantage-Master-Folder\vantage-app` is six weeks behind and follows the retired pre-`ai/` protocol; a session pointed at it boots on documents Phase 0 deleted. Delete it or mark it clearly.
- **Phase 1 is committed and pushed** as of 2026-08-30 (`25bd2ac`). **1.10 is NOT yet committed** — four modified files sit in the working tree on `C:`.
- **Deferred by Michael, 2026-08-30 — the orphan picker as a split pane.** Unchanged. If revived it needs a C8 and a §13.1 amendment, and the honest implementation is making the ONE editor hostable in two places — never a second inline editor. **Its list-scroll half is now available:** it is the same flex + `min-height:0` + sticky-`th` pattern 1.10 built, and the orphan window's `<thead>` already carries the shared class.
- **Needs Michael's eyes (small), carried from 1.9:** the 20-row search cap (C14-frozen) and the results list's 220px max-height.
- **Carried forward from 1.2, unchanged:** three drill attachments in `snapshots/files/` (`file-drill12-1..3`), safe to delete once snapshot `2026-08-29_101616.json` ages out.
- **Still unverified from here:** whether the *production* profile has its own backup folder and a green chip. No session has checked it yet.
- **Backlog (found, not done — out of compartment):** `parseCSVRow()` (~8770) does not handle escaped `""` inside a quoted field — carried from 1.3. `var(--color-danger)` is used in `renderDomainsView()` and defined nowhere, so the expired-domain highlight is silently dead — Media/Domain compartment. Both for 1.8. **Nothing new was found this session.**
- Carried forward, untouched: compliance obligations (§0), telemetry deferred to Phase 2, sheet/CSV import at scale, `Start_Vantage.bat` retirement, the two-inspector duplication (BUILD_NOTES open risk 3).
- **Closed, do not re-raise:** §15.1's reserved-vs-collapsed question (reserved, and it is verified); §15.3's tint percentage and the orphan window sharing it; §15.4's picker branch (deleted, `isOrphan` alone); the history backfill; §14.4's bulk question; TaskHub's cyan and its row coloring; snapshot cadence; chip placement; the task editor's prospect picker; §13.6's date picker; §13.8's hoisted inspector panel.

## Next step

Run **Session 1.11** (drag-to-reorder columns) from `ai/phases/phase-1-taskhub.md`, in a **new conversation**. It depends on 1.10 and on nothing else. Scope **§13.4** (reorder half); it extends **C16**'s second row, and **C15** and **C17** already exist and need no change.

**What 1.10 leaves it, so it does not need rediscovering:**

- **1.11's task 3 is already done.** `renderTaskHubTable()` builds its header *and* its cells from `taskHubColumns()`, so cells cannot drift out of sync with headers — the failure 1.11's own risk note calls "correct data under wrong labels". `TASKHUB_COLUMNS` is still the definition of what a column IS; `order` is only the sequence.
- **The 4px threshold branch is already in `initTaskHubHeaderDrag()`, with the comment saying exactly where the reorder goes.** 1.11 fills in live shifting and a drop indicator, and writes `order` on drop. It must NOT rewrite the handler.
- **Do not call `stopPropagation()` on the header click to suppress a sort.** The mechanism is the module-scope `taskSuppressNextHeaderClick` flag, set on mouseup by a drag and consumed by the sort listener — verified one-shot, so the click after a drag sorts normally.
- **Reordering must write `order` through `state.columnLayouts.taskhub` and call `saveState()` once, on drop** — never per mousemove. `setTaskHubColumnWidth()` is the shape to copy.
- The checkbox column and the **trailing spacer** carry no `data-col-key`; `taskHubHeaderCell()` returns null for both, which is what keeps them out of the drag with no special case anywhere.
- The reorder Done-when's ZIP round trip needs no new code — C17 already carries `order`, verified character-identical this session.

**Then 1.8 is the phase close and is always last.**

**Carry into 1.11 and later:** `#canvas-body` is still the shared scroll owner and is still untouched — a hub that wants to scroll internally fills it and owns its own overflow. In `style.css`, **check source order before assuming a new rule wins**: the TaskHub block sits ~950 lines above `.table-scroll-container` and `.premium-table`, and this session lost two rules to that at equal specificity, both presenting as "the sticky header is broken" rather than as a CSS bug.
