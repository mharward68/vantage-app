# AI Context — PHASE 3 IN FLIGHT, SESSION 3.3c SHIPPED

**Updated:** 2026-09-04 10:45 (America/New_York).

**Last run:** Phase 3 / **Session 3.3c — the authoring surface: toolbar, two disclosures, live preview.** Compartment: **UI**. ✅ **COMPLETE. No amendment, no frozen contract touched, no surprise in the mechanism.**

**State:** `app.js` **18,564** (+181) · `index.html` **3,981** (+61) · `style.css` **5,192** (+125). `CACHE_NAME` **v131 → v132**, **ONE bump** against a budget of two. **One-glance version tell:** console `typeof renderTaskBodyPreview` is `"function"` on v132 and `"undefined"` on v131. `typeof gmailSearchUrl` is `"undefined"` on both and must stay that way (A3).

**Git: ✅ COMMITTED AND PUSHED CLEAN, 2026-09-04 10:57 ET. HEAD is `d7d103fb`, parent `4a56a927` — BOTH READ FROM `.git/logs/HEAD` THIS SESSION, not carried.** The commit is *"Phase 3 Sessions 3.3 + 3.3c: email launch and the authoring surface"* and it covers **both** sessions: the four code files carried both sessions' changes in one working tree and could not honestly be split. `4a56a927` was 3.1b. **The working tree is clean as of this handoff; a later session must re-read the ref rather than repeat this number.** Michael runs git.

**Database: 1,561,663 bytes at boot and 1,561,663 bytes at the end — byte-identical.** 651 prospects · **0 tasks** · `taskSettings` still 2 keys (`dateMode`, `workGmailAddress`). A throwaway task was created through the shipped save path to prove the reload check and removed through the shipped `deleteTask()`; the byte count returning to its exact boot value is the evidence it left nothing. Only `vantage_prm_database` and `vantage_sidebar_pinned` in `localStorage`. `state.activeView` ended on `dashboard`, where it started.

---

## What was done — all four files

| # | Edit |
| --- | --- |
| 1 | `index.html`: 3.2's single `#task-msg-body-group` became **two stacked disclosures**. ⛔ **The id was KEPT on the first of the two** — `syncTaskMsgCounts()` finds the counter with `#task-msg-body-group .task-msg-count` and `syncTaskOutreachBlock()` re-attaches the detached Subject group with `.before()` on that node. Renaming it puts Subject below the body and kills a counter. New ids: `task-body-edit-toggle`, `task-body-edit-panel`, `task-md-toolbar`, `task-md-bold`, `task-md-italic`, `task-md-link`, `task-msg-preview-group`, `task-body-preview-toggle`, `task-body-preview-panel`, `task-msg-preview`. The textarea keeps `aria-label="Message"` because its `<label>` became the disclosure button. |
| 2 | `app.js § ✅ TASK EDITOR — AUTHORING SURFACE`, a new sub-block after `renderTaskOutreachActions()` and before `closeTaskEditor()` — so all six task blocks stay contiguous: `taskDisclosure`, `setTaskDisclosure`, `setTaskBodyDisclosureDefaults`, `toggleTaskDisclosure`, `renderTaskBodyPreview`, `taskBodyChanged`, `taskBodyWrapSelection`, `taskBodyInsertLink`. |
| 3 | Three hooks only: `fillTaskOutreachFromTask()` gained `setTaskBodyDisclosureDefaults(!task)`; `syncTaskOutreachBlock()` gained `renderTaskBodyPreview()` (the **channel** decides which of the converter's two outputs the preview shows); **seven** listeners in `setupEventListeners()`. |
| 4 | `style.css`: `✅ TASK EDITOR — AUTHORING SURFACE` after the 3.3 block. **`🧱 HUB SHELL` still last — S5 holds.** |
| 5 | `CACHE_NAME` v131 → v132. |

**No converter and no clipboard code was written.** The preview calls **3.3's** `outreachBodyToHtml()` and `outreachFlattenBody()`. That is the whole design: one function feeds the preview and the clipboard, so "what you see is what pastes" is structural rather than tested.

## Verification — all of it ran

- **Defaults inverted, twice, and the second time on a REAL SAVED RECORD.** New task → Edit `is-open` / `aria-expanded="true"` / panel `block`, Preview `false` / `none`. Existing task → exactly the reverse. Set on every open from `!!task`, so the last edit's state is irrelevant.
- **Stored NOWHERE.** Across a collapse of both boxes: `Object.keys(state).length` 38→38, `taskSettings` keys `dateMode|workGmailAddress` unchanged, `localStorage` keys `vantage_prm_database|vantage_sidebar_pinned` unchanged, `vantage_prm_database` 1,561,663→1,561,663 bytes, `generateSettingsCSV()` **4,568 chars / 99 rows / hash −203735693 — identical either side**. None of `task-msg-preview-group`, `task-disclosure`, `bodyDisclosure` appears anywhere in `JSON.stringify(state)`.
- **Toolbar, exact selection.** `"Hello there, Sally"` sel 6–11 → **B** → `"Hello **there**, Sally"`, selection 8–13 still exactly `there`. **I** → `"Hello *there*, Sally"`, selection 7–12. **B on an empty caret** at 5 → `"Hello**** there"`, caret at 7, between the markers. **Link** on sel 8–16 of `"See the Tech RFP for details."` → `"See the [Tech RFP](…) for details."`, selection 9–17 still exactly `Tech RFP`. **Link with nothing selected** makes the URL its own label and leaves it selected. **Cancel writes nothing** (`"untouched"` unchanged). **A URL containing a space is REFUSED** with a toast, because `OUTREACH_MD_RE`'s URL run is `[^)\s]+` and it would have stored as text that never converts.
- ⛔ **PREVIEW `innerHTML` vs CLIPBOARD `text/html` — READ THIS BEFORE RE-RUNNING IT.** On a body with a link, bold, italic, two blank lines, `&`, `<` and `>`: **byte-identical, 206 === 206, `firstDiffAt: -1`.** Repeated on the real saved record: **223 === 223.** **On a body containing a `"` they are NOT string-equal and that is the DOM, not a drift** — `outreachEscapeHtml` writes `&quot;`, and the browser re-serialises a text node's `"` unescaped, so the only difference in 80 vs 70 chars is at index 9, `&quot;an` vs `"any tim`. Proved with `scratch.innerHTML = payload.html` → **`box.innerHTML === scratch.innerHTML` in both cases**: the preview is showing precisely the clipboard string.
- **LinkedIn preview is flattened.** `box.textContent === outreachClipboardPayload("linkedin", body).text` → true; `payload.html === null`; no tags in `innerHTML`; no `**` and no `](` in the text. The counter followed the output: stored 157, flattened 150, counter read **150/300** on `connect`.
- **Reload → `msgBody` unchanged.** Saved through the shipped `saveTaskFromEditor()`, page reloaded, task reopened: hash `574833429`, length 180, **identical to the editor value before the save and to the stored record**.
- **`channel: ""` regresses clean.** `#task-outreach-fields` `hidden`, computed `display: none`, 0 visible toolbar buttons, 0 visible preview boxes, 0 launch controls — the two disclosures exist in the markup and nothing about them is on screen.
- **Clean five-line boot console, zero errors and zero warnings, no favicon 404.** All six views render with a definite height — five at `997.333px`, MediaHub at `977.333px`, which is its known horizontal-scrollbar case and not a regression.
- **`node --check` parses and `check_ids.py` is at baseline** (`{export-backup-btn, restore-backup-input}`), both run **against the files as they sit on the mini-PC**, md5-matched to the bytes the browser ran: `app.js 2489ae1d…`, `index.html 85023833…`, `style.css a6764a73…`, `sw.js 1679f391…`.

## Backup coverage — DIRECTIVES §4

**NO STORE WAS ADDED OR MODIFIED, AND THAT IS THE WHOLE ANSWER.** The open/closed state is a CSS class on a node the editor rebuilds — no `state` field, no `localStorage` key, no settings row, no CSV column, no `wipeAllData()` line. `TASKS_CSV_HEADERS.length` is untouched and contract Q8 stands. Nothing this session added is user-writable data.

## Assumptions logged this session

1. **The defaults are set inside `fillTaskOutreachFromTask(task)`**, which `openTaskEditor()` calls last, rather than beside `openTaskEditor()`'s own `isNew`. Same value (`!!task`), same call, and it keeps every outreach concern inside the outreach block. Reversible.
2. **The toolbar dispatches a real `input` event after writing.** Assigning `.value` fires nothing, so the counter, the launch controls and the preview would all sit stale. Dispatching keeps typing and clicking **B** on one code path instead of two.
3. **A `prompt()` supplies the link URL**, per the plan. The codebase already prompts on `wipeAllData()`; a bespoke modal inside an open modal for one string is not worth it.
4. **The preview's anchors are click-prevented.** They must stay real `<a href>` for the byte-equality above, so a delegated listener on `#task-msg-preview` calls `preventDefault()` — Q8/P9, no message content in an address bar.
5. **A link colour is hard-coded `#7aa7ff` / `#1a56db`, deliberately not `var(--color-primary)`**, which is the hub's colour and would turn the preview's link orange in MediaHub. The preview imitates Gmail; it must not follow the hub.
6. **A throwaway task was created and deleted through the shipped paths** to prove "reload; `msgBody` unchanged", which cannot be proved on a database with 0 tasks. The database returning to its exact boot byte count is the evidence it left nothing.

## Open items

- ✅ **COMMIT — DONE.** `d7d103fb` on `main`, pushed clean, covering 3.3 and 3.3c together. **This retires an open item that had been carried at the top of the handoff since 3.1.** The only thing written after the push is this file, whose git-state paragraph was corrected in the same session that read the ref.
- ⛔ **`michaelh@youravdept.com` IS STILL NOT SIGNED IN TO THE VERIFYING CHROME PROFILE.** The compose button lands on Google's sign-in page with the payload intact until he signs in. **3.4 or the 3.5 close still owes the one confirmation that it reaches the work inbox** — this session did not open Gmail and does not discharge it.
- **Two stray Gmail drafts** in the personal account from 3.3's verification. Left in place.
- **Next is 3.4 — LinkedIn launch** (UI, M, ~10 min, Confidence High): `linkedinSlug()` reading `prospect.linkedin`, the three destinations, the profile-URL fallback shipped the same session, the copy controls. **Its input is the account tier — free or Premium — because that gates whether `connect` is built at all.** 3.3's close recorded **Premium confirmed, so `connect` IS BUILT**; re-confirm before writing it. **3.4 is the review point.**
- **Phase 2B is still not formally closed** — its snapshot re-verify has not run.
- **Phase 2C, scoped not built:** the sixteen-store `wipeAllData()` gap and the `ensureStateDefaults()` `length === 0` reseed defect.
- **The snapshot system is alive** — every boot read a snapshot off disk; `#snapshot-restore-select` now offers **16** files.
- **Still owed by Michael, an EIGHTH close carrying them:** the five `DECISIONS.md` DECLARATIONS amendments; the two from 2A.6; the domain-is-identity amendment; the 2B.16/2B.17 P4/P5 divergence amendments. `LA` = Louisiana or Los Angeles. Finding 10d's meaning. Three cosmetics from 2B.4 — **leaving them is a valid answer and saying so closes them.** ⚠️ **A4 still owes a `DECISIONS.md` entry.**
- **`ai/phases/phase-2b-RUNSHEET.md` is spent and marked for deletion. Michael deletes it.**
- **`DECLARATIONS.md` Stack line counts are stale** — it says `app.js` ~13,270; real is **18,564**. Propose at the 3.5 close; do not edit mid-phase.
- **Phase backlog, carried:** the `"Note"` reachout-type reassignment gap; ProspectHub OR-s tags while Advanced Query AND-s them; `p.notes` / `p.location` shape defaults; company-edit uniqueness guard; the §4 back-fill of existing `"domain.com"` rows; the missing `<link rel="icon">`. **The repo is PUBLIC** and DIRECTIVES §0 compliance is undecided. Stale `..\backups\`.
- **Both query surfaces stay DEFERRED**; the `sequences` tab stays `enabled: false`. Neither was touched.
- **The enrollment compartment still has no scope.** Phase 3 needs a Prompt 1 intake before 3.6+ can exist. Sixth close to say so.

## Estimate vs actual

Sized **M / ~8 min / High**, and **all three were right — this is the first session in the phase where nothing had to be re-sized.** The split that created it is the reason: 3.3 kept every mechanism that fails invisibly and 3.3c inherited a converter already proved in real Gmail, so the only new failure available was cosmetic and none occurred. **One `CACHE_NAME` bump against a budget of two, and the first Phase 3 session to finish on one.** **Michael's time was ~2 minutes** — two boot answers and no interruption after that. The one judgement call, the `&quot;` string equality, was settled on DIRECTIVES §5's ladder without needing him.

## Files changed

**Code:** `app.js`, `index.html`, `style.css`, `sw.js`. **Documents:** `ai/AIContext.md`, `ai/archive/2026-09-04_1045_AIContext.md` (new), `ai/BUILD_NOTES.md`.

## ⛔ EXACT NEXT STEP — **SESSION 3.4**

Commit, then run **3.4 — LinkedIn launch** from `ai/phases/phase-3-outreach-launch.md`. **Read A1, A2, A3 and A4 before trusting Q1–Q7.** 3.4 writes no converter: `outreachClipboardPayload("linkedin", body)` already returns `{text, html: null}` and 3.3 proved it; 3.4's copy controls call that function and nothing else. **Confirm the LinkedIn account tier first — it decides whether `connect` is built.** **3.4 is the review point; expect it to produce sessions.**

**Carry forward:** ⛔ **`#view-data-management`'s NODE COUNT IS NOT A CONSTANT — 68 IS ALREADY STALE AND IT READ 69 THIS SESSION WITH NOTHING OF MINE INSIDE IT.** The mover is `#snapshot-restore-select`, one `<option>` per snapshot file on disk: 16 options now against 15 at 3.3. **The stable number is `total − options` = 53.** ⚠️ **The extension's content filter blocks a result key containing the word "session"** — `nodes_from_this_session_inside_it` came back `[BLOCKED: Sensitive key]` twice before the key was renamed. ⚠️ **`javascript_tool` returns `[BLOCKED: …]` for a raw URL or HTML string** — split on `//`, `?` and `=`. ⚠️ **`navigator.clipboard.readText()` HANGS the tool for 45s.** **Write each revision to a NEW staged path and confirm the device file SIZE before believing a commit.** **THE APP RUNS PERFECTLY WITH THE SERVER DOWN** — three-probe first; it read `200 / 404 / 200` this session. **The first load after a bump serves the OLD document** — it did again here, `caches.keys()` already read v132 while the page still ran v131. **`state` is not `window.state`.** **Do not open the app while Michael's window is up.** **TEST AGAINST REAL RECORDS, WRITE UP PLACEHOLDERS — the repo is public.**
