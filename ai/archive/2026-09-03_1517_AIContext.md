# AI Context

**Updated:** 2026-09-03 14:54 (America/New_York) — session closed here.

**Last run:** Phase 2B / **Session 2B.14 — Include semantics: OR within a picker, AND across pickers.** Compartments: ProspectHub directory + Advanced Query modal, **plus MediaHub and CampaignHub under the cross-compartment exception granted 2026-09-02 at full scope.** Plan: `ai/phases/phase-2b-review-response-plan.md` **line 626**. Finding 5.
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two (`{'export-backup-btn', 'restore-backup-input'}`) · `CACHE_NAME` **v124 → v125, CONFIRMED LIVE** (`caches.keys()` `['vantageprm-cache-v125']`, `controlled: true`, and — the check that actually matters — the RUNNING function bodies read `includeSet).some(`) · `app.js` 17,180 → **17,188** (+8 net; roughly 90 lines added, ~80 of them comment, against ~80 deleted) · **`index.html` 3,769 and `style.css` 4,939 — BOTH UNTOUCHED. This is the first session in the phase to change only `app.js` and `sw.js`.** · **console: 5 lines, one boot, ZERO errors** (`onlyErrors` empty; the one WARNING is the pre-existing zero-byte-snapshot pruner, see Open items) · **THE PRODUCTION DATABASE IS LOADED — 651 prospects / 1,090 companies / 33 media / 3 campaigns / 4 audiences / 31 tasks** · `localStorage` **1,568,519 at open → 1,568,520 at close (+1 byte, unattributed — see Assumption 4)** · git **UNCOMMITTED — `app.js`, `sw.js` and the three `ai/` documents are outstanding. Last commit is `50253cc`.** ⛔ Git is always Michael-runs-it on this machine; I did not run it and this line is not a claim that I checked. BUILD_NOTES **§ Git, from a session with no shell** · deployed n/a.
**Estimate vs actual:** sized **M / ~10 min / Medium**. **It ran M** — the surface count was the work, exactly as the plan predicted, and the one thing the plan did not have (the ProspectHub classification collapse, Assumption 1) was found by reading rather than by failing. **Michael's time: ~4 minutes** — one boot question block of two, one "production database is loaded", one "is this finished".

**One-glance version tell:** in ProspectHub, pick **two** tags in the tag filter. **The counts go UP.** `7-7-26 Auto-eight` + `Tumbler Audience` → **Contacts (11), Companies (10)**. If it reads Contacts (1), you are on v124.

## ⚠️ THE PLAN, THE FINDINGS AND THE RUN SHEET ALL MISNAME THE ACCEPTANCE TAG

**The tag is `7-7-26 Auto-eight`. Every document says `7-7-28`.** It is a prospect tag carried by 11 records; there is no tag containing "7-7-28" anywhere in the database. **The test still produced exactly the predicted numbers once the real label was used.** Worth correcting in the review-response plan and `phase-2b-REVIEW-FINDINGS.md` at 2B.10 — a future session that greps for the printed string finds nothing and has no way to tell a typo from a deleted tag.

## What was done

Include changed from `.every()` to `.some()` at **every** site in the Finding 5 table. **Exclude was not touched anywhere and is still `.some()`.**

| Surface | Function | What changed |
| --- | --- | --- |
| AQ modal — Tags, Campaigns, Audiences, Industry | `matchesIncludeExclude` | include `.every` → `.some` |
| AQ modal — **Title** | `matchesIncludeExcludeSmart` | include `.every` → `.some` ← **the trap; two functions, not one** |
| AQ modal — company-tags text field | `matchesTagsFilter` | `terms.every` → `terms.some` |
| ProspectHub — both directory predicates | `renderProspectsView()` | **the classification pass deleted** — see Assumption 1 |
| MediaHub tag rail | `renderMediaView()` | `hasAllTags` → `hasAnyTag` |
| CampaignHub tags | `renderCampaignsView()` | `hasAllTags` → `hasAnyTag` |

**Untouched, as required:** `renderAqInspectorDrawer()`, every `aq-insp-*` id, the Audience Query Engine (`runCampaignQuery()`, ~11,966), `exportAqRecordsCSV()`, `prospectTagFilterTerms()`, `PROSPECT_TAG_PICKER`, `AQ_PICKERS`, `#prospect-tag-chooser`. **No frozen contract was modified** — P8 is the id-and-accessor contract and this session changed only what the filter *means*.

## Verified — real output, not a claim

**THE ACCEPTANCE TEST, driven through `setAqPickerSelection()` — the same function the chip UI calls:**

```
Advanced Query / target Prospect / Tags picker
  '7-7-26 Auto-eight' alone   -> 11
  'Tumbler Audience' alone    -> 1
  BOTH included               -> 11   distinct ids 11   'Michael Harward' rows 1
  monotonic (both >= each)    -> true
```
Screenshotted: the Query Results window reads **"Showing 1-11 of 11 results"**.

**THE TITLE PICKER — proof the SECOND function was changed, which is the whole trap:**

```
  Title {Director}            -> 549
  Title {Manager}             -> 2
  Title {Director, Manager}   -> 550    broadened: true
```

**CROSS-PICKER STILL NARROWS, AND EXCLUDE STILL REMOVES ROWS:**

```
  Tags {Auto-eight}                              -> 11
  Tags {Auto-eight, Tumbler} + Title {Director}  -> 8     fewer than tags alone: true
  Title include {Director}                       -> 549
  Title include {Director} exclude {Senior}      -> 483   removed 66 rows
```

**PROSPECTHUB DIRECTORY — the on-screen `#contacts-count` / `#companies-count`, screenshotted:**

```
  7-7-26 auto-eight                     -> contacts 11, companies 10
  tumbler audience                      -> contacts  1, companies  1
  7-7-26 auto-eight + tumbler audience  -> contacts 11, companies 10

THE MIXED CASE — one PROSPECT tag + one COMPANY tag, which a mechanical
.every->.some would STILL have narrowed:
  av company                            -> contacts  0, companies  2
  7-7-26 auto-eight + av company        -> contacts 11, companies 12   <- the union
```

**MEDIAHUB AND CAMPAIGNHUB:**

```
  MediaHub  {Swipe} -> 5   {Engagement} -> 3   both -> 7    (v124 gave the intersection, 1)
  CampaignHub  {Tumbler} -> 1   {Tumbler, __no-such-tag__} -> 1   (v124 would give 0)
```

**STATIC PROOF ON THE SHIPPED SOURCE, before the browser ran at all** — the three helper bodies were lifted out of `app.js` by text and evaluated in Node: an exhaustive sweep of **448 (record, include-set, added-term) triples** produced **0** cases that passed and then failed after a term was added.

## Assumptions logged this session

1. ⚠️⚠️ **THE PROSPECTHUB CLASSIFICATION PASS WAS DELETED, AND THIS IS THE ONE THING THE PLAN DID NOT HAVE.** `renderProspectsView()` sorted each picker term into `companyTagQueries` and `prospectTagQueries`, required each list in full, then **AND-ed the two results** — with a third "both lists" branch to stop a dual-nature tag being demanded twice. **A mechanical `.every()` → `.some()` leaves that `&&` in place, so one company tag plus one prospect tag still narrows** — and it passes any spot-check that uses two tags of the same kind. `#prospect-tag-chooser` is ONE picker (its options list mixes company and prospect tags), so all its terms are OR-ed; once they are, the classification has nothing left to decide. Both predicates collapsed to one `.some()` per record over the record's own tags and its company's. Two render-scoped `Map`s (`companyTagsById`, `contactTagsByCompanyId`) replace the per-record `find()`. **Reversible in git; the deleted logic is described in full in the comment that replaced it.**
2. **MediaHub and CampaignHub were changed**, on the exception recorded as *"GRANTED 2026-09-02, full scope. It covered 2B.13 and covers 2B.14 and 2B.18."* Leaving them would have left two hubs silently disagreeing with everything else.
3. **CampaignHub was proved with a term nothing carries** (`__no-such-tag__`), because the real data holds exactly one campaign tag label. Under `.every()` that returns 0; it returned 1. Decisive, and it wrote nothing.
4. **The +1 byte in `localStorage` is unattributed.** I did not capture a per-key baseline at open, so I can say only that every operation this session ran is a read-only filter and that `switchView()` calls `saveState()`. **Recording the per-key lengths now so the next session has the baseline I lacked:** companies 1,107,581 · prospects 401,600 · media 42,617 · campaigns 729 · tasks 6,903 · audienceLists 2,076.

## Backup coverage — DIRECTIVES §4

**NO NEW STORE, NO NEW FIELD, NO NEW KEY, NO MIGRATION, NO RECORD WRITTEN. This session changes only how existing records are SELECTED for display.** `ensureStateDefaults()`, `wipeAllData()`, every CSV writer and the whole restore path are **byte-unchanged**. No manual ZIP was taken and none was required — the run sheet's backup points list 2B.19, 2B.18, 2B.20, 2B.17 and 2B.10, not this session.

⚠️ **One thing worth knowing rather than fixing: the filtered-set exports now export MORE.** `lastFilteredProspects` / `lastFilteredCompanies` feed Export CSV, and a two-tag filter now yields a union rather than an intersection. That is the feature, not a defect — but the first export after this change will be bigger than the same selection produced yesterday.

## Open items

- **⚠️ NEEDS YOUR EYES — the first real query.** Results get **bigger**; that is the change, not a broken filter. Specifically: **your saved habits around multi-tag filtering now mean the opposite of what they meant.** If you were picking two tags to narrow, you now need two *pickers*.
- **⚠️ THE ACCEPTANCE TAG IS `7-7-26`, NOT `7-7-28`, IN ALL THREE PLANNING DOCUMENTS.** See the heading above. A one-character correction owed at 2B.10.
- ⚠️ **ONE SNAPSHOT FILE IS STILL BEING WRITTEN ZERO-BYTE AND THE PRUNER STILL CLEANS IT UP.** `[Snapshot] Ignoring 1 zero-byte snapshot file(s)` then `Pruned 1 file(s) — 1 zero-byte, 0 aged out. 15 kept.` at 14:49:22. **Third session running. Pre-existing, self-healing, and a truncated snapshot write is a Tier-1 recovery event.** Not mine; not investigated.
- **The snapshot chip reads "Not protected"** in the sidebar on a database that has a snapshot from 14:40:53 on disk. Same display defect already on the backlog (`renderSnapshotHealthChip()` / `computeSnapshotState()`), now visible in a screenshot.
- **Still owed by you:** the **P8 revision** (gates 2B.15) · the **2B.17 layout design** · and **Finding 10d's meaning** in one word.
- **⛔ 2B.10 OWES FOUR AMENDMENTS NOW, IN ONE BATCH:** 2B.21's P9 (data), 2B.17's P5, 2B.22's P9 (the `#modal-prospect` layout override), **and Finding 5's own note that this change REVERSES what 2B.9 verified and shipped — it is a decision, not a regression fix, and nobody should go hunting for a break that was never there.**
- **Phase backlog:** the cosmetic `p.notes` shape default; a `p.location` shape default; the uniqueness guard on the company edit path; the snapshot chip display defect; ZIP restore reports 1 orphaned task on a round trip; `.col-resize-handle` keeps the stock cursor; the §4 back-fill of existing `"domain.com"` and slug rows. **Removed from the backlog: MediaHub and CampaignHub tag semantics — done this session.**
- **Carried, unchanged:** `#btn-see-all-contacts` does not exist (it is `btn-show-all-contacts`). Both `forceShowAll*` true blanks the directory. CampaignHub identifies itself twice. MediaHub's tag rail off the right edge. `state.columnLayouts.taskhub.widths` carries `firstName: 0` and `lastName: 0`.
- **Unchanged from Phase 1:** `parseCSVRow()` `""` gap; repo is PUBLIC; DIRECTIVES §0 compliance undecided; stale `..\backups\`; `schema_update.sql` still deletable.

## Files changed

**Code:** `app.js` (17,180 → **17,188**), `sw.js` (**v124 → v125**, one bump, and it held). **`index.html` and `style.css` untouched.**

**Documents:** `ai/AIContext.md`, `ai/archive/2026-09-03_1454_AIContext.md` (new), `ai/BUILD_NOTES.md`.

**No `DECLARATIONS.md` or `DECISIONS.md` change.**

## Next step

**2B.15** — but it is **BLOCKED on the P8 revision**, which is still owed. If that is not ready, run **2B.11** or **2B.12**, both unblocked. **2B.10 is ALWAYS LAST**, and it opens by writing the four owed amendments in one batch.

⚠️ **COMMIT FIRST.** `app.js`, `sw.js` and the three `ai/` documents are outstanding against `50253cc`.

**Carry forward:** the `🧱 HUB SHELL` block stays LAST in `style.css`. `#canvas-body` is never edited. `state` is not `window.state`. `state.selectedProspectId` and `detailProspectId` are two cursors. No routing, ever. `switchView()` calls `saveState()` — **do not navigate inside a byte-comparison window** (and capture per-key lengths at open, not just the total — see Assumption 4). Inject the transition kill-switch after every reload. The theme button is **`#theme-toggle-btn`**. `resize_window` does not work on this machine. **`setAdvancedQueryTarget()` takes `"prospect"` / `"company"` — SINGULAR. Passing `"prospects"` silently runs the COMPANY branch and returns 1,090 rows that look like a plausible answer.** **Both query surfaces stay DEFERRED.** `wipeIndexedDB()` clears only the `files` store, NOT `handles`.
