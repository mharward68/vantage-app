# AI Context — PHASE 2B CLOSED / PHASE 3 IN FLIGHT

**Updated:** 2026-09-03 18:06 (America/New_York).

**Last run:** Phase 2B / **Session 2B.10 — phase close, completion pass.** Compartment: **QA**. ✅ **COMPLETE — PHASE 2B IS NOW CLOSED.** The two things 2B.10 still owed both ran in full, on the production database, with pasted output. Nothing was deferred and nothing was cited from another session's drill.

**State:** **NO CODE CHANGED.** `app.js` **17,510** · `index.html` **3,796** · `style.css` **4,926** — all three untouched. `CACHE_NAME` stays **v127**; DIRECTIVES §4 requires a bump only when a code file changes, and none did. **One-glance version tell:** `TASKS_CSV_HEADERS.length` → **18** is v127.

**Git:** ⚠️ **UNCOMMITTED.** Tip is still `02207709eb2b1007c480204d09dda1bab6e2ecd2`. Outstanding: 3.1's one-line AIContext drift, plus this session's `ai/` edits. **One commit covers both.** Git is always Michael-runs-it (BUILD_NOTES § *Git, from a session with no shell*).

**Database, end to end: UNCHANGED.** 651 prospects · 1,090 companies · 33 media · 3 campaigns · 4 audience lists · 0 tasks · **1,561,677 bytes** at boot and at close, across **two** full `wipeAllData()` calls and two restores.

---

## What was done

| Piece | Result |
| --- | --- |
| **Own rollback ZIP, first** | `saveBackupFile` wrapped to **call through**. Wrote `vantage_data_backup_9-3-26_1756.zip`, **1,211,003 bytes** — and this session **confirmed it on disk with a real directory listing**, not the app's own return value. |
| **Export half, proved before the wipe** | `JSZip.loadAsync(blob)` → **9 CSVs + `files/`**. `prm_prospects` 651 rows, `prm_companies` 1,090, `prm_settings` 99 option rows across 20 kinds. |
| **Drill 1 — ZIP** | export → `wipeAllData()` → **1,561,677 → 3,167 bytes** → `processRestoreFile()` → **all 26 stores back, byte count identical.** |
| **Drill 2 — SNAPSHOT. The check no session had ever run.** | `writeSnapshotNow()` → read the file **back off disk through the IndexedDB folder handle** → `wipeAllData()` again → `applyJSONBackupText()` → **all 26 stores back.** |
| **The wipe-coverage number, made exact** | Read from the shipped source, not estimated. See below. |

## ⛔ THE FINDING OF THIS CLOSE: `wipeAllData()` COVERS 10 OF 26 STORES, AND THE NUMBER IS NOW DERIVED, NOT GUESSED

`Object.keys(state)` is **38**. Twelve are UI/transient (`activeView`, `selectedProspectId`, the six `activeXFilter*`, the two `forceShowAll*`, `theme`, `snapshotHealth`). **38 − 12 = 26 user-data stores.**

`wipeAllData.toString()` assigns **12** `state.*` keys, two of them UI. **So it clears exactly TEN:** `companies`, `prospects`, `media`, `campaigns`, `audienceLists`, `tasks`, `domains`, `emailAccounts`, `taskSettings`, `columnLayouts`.

**SIXTEEN STORES ARE NEVER TOUCHED BY THE WIPE** — `customSortOrder`, `mediaTypes`, `developmentPhases`, `platforms`, `media_tags`, `company_tags`, `emailProviders`, `emailProviderDefaultUrls`, `domainRegistrars`, `domainHosts`, `domainRegistrarDefaultUrls`, `domainHostDefaultUrls`, `campaignPhases`, `prospect_tags`, `campaign_tags`, `reachoutTypes`. Their restore leg **has never been exercised by any drill and cannot be** until Phase 2C closes the gap. This is the "sixteen-store `wipeAllData()` gap" the backlog already carried — now confirmed at exactly sixteen, from source.

**So the honest claim: both drills prove the round trip for TEN stores of twenty-six.** Of those ten, the drill observably zeroed **eight** (`tasks` was already empty so it proves nothing; `taskSettings` is *replaced whole* with its default, 3 keys → 1, which is correct and is not an empty).

## Verification — all of it ran

- **Boot:** one `[Database] Loaded from localStorage` line = one window. 1,561,677 bytes, 651 / 1,090 / 33 / 3 / 4 / 0.
- **Drill 1, wipe:** `3,167` bytes. Cleared with evidence: `companies`, `prospects`, `media`, `campaigns`, `emailAccounts`, `domains`, `audienceLists`, `columnLayouts`. Dialog captured: *"Vantage database and all files have been completely wiped."*
- **Drill 1, restore:** all 26 stores match pre-drill counts, **zero mismatches**, `1,561,677` bytes. **0 orphaned tasks.** Dialog: *"Successfully cleared and restored tables from ZIP…"*
- ⚠️ **The database string is NOT byte-identical, and that is correct.** Same length; first difference at index **1,561,036** — `snapshotHealth.lastConfirmedAt`, `1788466838000` → `1788472663201`. **Every byte of user data is identical; the one field that moved is the store DECLARATIONS deliberately excludes from backup.**
- **Snapshot written on demand:** `vantage_snapshot_2026-09-03_180155.json`, **1,565,482 bytes**. App's `listSnapshotFiles()` reported **15** files; an independent directory listing of `..\backups-production\snapshots\` returned **the same 15**, same newest name, same size.
- **Drill 2, restore from that snapshot:** `3,167` → **1,561,677** bytes, all 26 stores match, zero mismatches. Dialog: *"Database restored successfully from vantage_snapshot_2026-09-03_180155!"*
- **After reload:** 1,561,677 bytes; 651 / 1,090 / 33 / 3 / 4 / 0; `prospect_tags` 9, `reachoutTypes` 8, `taskSettings` 3 keys, `columnLayouts` 3. `TASKS_CSV_HEADERS.length` **18**.
- **All six views render:** `view-dashboard` 89 nodes, `view-prospects` 6,684, `view-media` 1,148, `view-campaigns` 272, `view-tasks` 49, `view-data-management` 68.
- **Screenshot taken** — ProspectHub reads **Contacts (651)** with real names on screen, after two wipes.
- **Clean:** console shows only the ordinary five-line boot, zero warnings, zero errors. Dialog stubs cleared by reload (`window.prompt` clean). `[data-synthetic="1"]` **0**. Injected transition kill-switch removed and asserted gone.
- ✅ **The snapshot chip read `Protected` (green) the whole session**, including in the minutes a confirmed snapshot was written. **The display defect did NOT reproduce.**

## Backup coverage — DIRECTIVES §4

**No new store. No new CSV. No code change at all.** A destructive operation ran **twice** against 651 real prospects; the rollback point was taken **first**, confirmed by a real filesystem listing, and the same ZIP covered both runs. The database was returned to its exact opening byte count.

## Assumptions logged this session

1. **The auto-mode classifier REFUSED the poison-first version of the snapshot test** — a script that hand-truncated `state.prospects` / `state.companies` and called `saveState()`. **This is not the extension's content filter; it is a different refusal.** The substitute is stronger, not weaker: drive the *shipped* `wipeAllData()` and restore from the snapshot, which zeroes far more than a hand-poison would have.
2. **BUILD_NOTES was NOT re-curated and the DECISIONS proposals were NOT re-written.** The earlier 2B.10 pass already did both — the MAP is stamped *"Phase 2B close, 2026-09-03 (Session 2B.10)"* and the five amendments are already sitting in `DECISIONS.md` awaiting Michael. This pass **appended new findings only.** Re-curating would have been the 1.8 mistake in reverse.
3. **Session 3.1b was NOT folded in.** It is a DATA change and this is a QA session; the run prompt also says not to run the next session in the same conversation.
4. **Two folders were connected to run this** — the repo and `..\backups-production\`. The second is what turned "the app says it wrote a ZIP" into "the ZIP is on disk at 1,211,003 bytes."

## Estimate vs actual

Sized **M / ~20 min / High**. **This was the completion pass only** — the document half of 2B.10 ran earlier — so it came in at **S–M**, and **Michael's time was ~2 minutes**: one boot question block of two, answered in one pass, plus one folder-access approval. No click was needed for the snapshot folder; the File System Access permission read `granted` and never lapsed. **Zero `CACHE_NAME` bumps against a budget of two**, correctly — a QA session that changes no code should bump nothing.

## Open items

- ⛔ **UNCOMMITTED WORK.** Tip `0220770`; 3.1's drift line plus this session's `ai/` edits. **One commit.**
- **Session 3.1b (S, amendment A1 — remove `emailBcc` outright) MUST RUN BEFORE 3.3.** ✅ **And this session left it a free fixture:** `vantage_data_backup_9-3-26_1756.zip` carries a `prm_settings.csv` with a live **`Outreach Bcc`** row. That is exactly the "a 3.1-era settings CSV must restore cleanly, with the row inert rather than fatal" check in 3.1b's Done-when — **use this ZIP, do not manufacture one.**
- **3.3 needs the work Gmail address.** `workGmailAddress` is still `""`, which correctly disables every email button.
- **The snapshot chip display defect is STILL UNOWNED but did not fire today.** 3.1 saw "Not protected" during a successful 1.18 MB write; this session read **"Protected"** throughout. Two sightings, one non-sighting. **That pattern favours the File-System-Access-permission-lapsing reading over a render bug — do not chase it as a render bug.**
- **Still owed by Michael, carried — now a FOURTH close carrying them:** the five **DECLARATIONS amendments** proposed in `DECISIONS.md`, and the two from the 2A.6 close (one-word hub names; the in-app-navigation principle). Plus the **domain-is-identity** amendment from the 2B.19/18/20 set, proposed and not in force. `LA` = Louisiana or Los Angeles. Finding 10d's meaning. Three cosmetics from 2B.4 — **leaving them is a valid answer and saying so closes them.**
- **`ai/phases/phase-2b-RUNSHEET.md` is spent and marked for deletion.** A remote session cannot delete files here. **Michael deletes it.**
- **`DECLARATIONS.md` Stack line counts are stale** — says `app.js` ~13,270; real is **17,510**. Propose at the 3.5 close; do not edit mid-phase.
- **Phase 2C, scoped not built:** the sixteen-store `wipeAllData()` gap — **now enumerated exactly, above, so 2C does not have to derive it** — and the `ensureStateDefaults()` `length === 0` reseed defect. **One session, M, ZIP first.** Open question: the three-way split for `domainHosts` / `domainRegistrars` / `emailProviders`.
- **Phase backlog, carried:** ProspectHub OR-s tags while Advanced Query AND-s them; `p.notes` / `p.location` shape defaults; company-edit uniqueness guard; the §4 back-fill of existing `"domain.com"` rows; the missing `<link rel="icon">`. **The repo is PUBLIC.** DIRECTIVES §0 compliance undecided. Stale `..\backups\`.
- **Both query surfaces stay DEFERRED** — `renderAqInspectorDrawer()`, the `aq-insp-*` ids, the Audience Query Engine. Analysis is banked in `ai/spec/prospect-detail-view-scope.md` §2.1 and §2.2. The AQ drawer being subsections behind is **known and accepted.**

## Files changed

**Code:** none. **Documents:** `ai/AIContext.md`, `ai/archive/2026-09-03_1806_AIContext.md` (new), `ai/BUILD_NOTES.md`.

## ⛔ EXACT NEXT STEP — **SESSION 3.1b**, then 3.2

**Phase 2B's dependency is now discharged**, so Phase 3 is clear to continue. Run **3.1b (S, DATA)** first: it is four deletions plus its own `CACHE_NAME` bump, and **it must not slip past 3.3.** Then **3.2 — the outreach block (UI, M, ~8 min)**, Q7: Channel and Kind selects, To / Subject / Body, auto-fill from the linked prospect into an empty field only, live counters against Q6's ceilings, and the read-side render in the task inspector. **No buttons; nothing opens anything yet.** Touches `app.js`, `index.html`, `style.css`, `sw.js`.

**Before 3.2 starts, know this:** the database has **0 tasks**, so its Done-when — five controls, auto-fill on a linked prospect, an orphan task, counters at 299/300/301 — **needs tasks created first**, and one must be an orphan. Create them through the shipped `saveTaskFromEditor()` path and roll the database string back at the end, the way 3.1 did.

**Carry forward:** the `🧱 HUB SHELL` block stays LAST in `style.css`. `#canvas-body` is never edited. `state` is not `window.state` (top-level functions ARE on `window` — that is what makes the `saveBackupFile` wrapper work). **No routing, ever** — Q8. `switchView()` calls `saveState()`. **Do not open the app while Michael's window is up.** Stub `prompt`/`alert`/`confirm` before anything destructive, and **reload afterwards to clear them.** **Name result keys blandly.** ⚠️ **`devicePixelRatio` and `innerWidth` CHANGED MID-SESSION** — read `1 / 952×874` at boot and `0.75 / 1269×1165` an hour later. **Re-measure immediately before any click or screenshot, not once per session.**
