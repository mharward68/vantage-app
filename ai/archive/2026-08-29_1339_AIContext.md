# AI Context

**Updated:** 2026-08-29 12:02 (America/New_York)
**Last run:** Phase 1 / Session 1.5 — TaskHub view: nav, color, filters, sortable table, pagination  **Compartment(s):** UI
**State:** builds ✅ · app loads with a clean console (no app errors) · deployed n/a
**Estimate vs actual:** planned **L / ~10 min of Michael's time**. Actual **L**, ~**2 min** — BOOT report plus the end-of-session read. The plan said no inputs needed and none were. Nothing escalated, no three-strikes, no mid-run blockers. Two "Needs my eyes" items are queued below rather than having interrupted the run.

## Done

- **TaskHub is the sixth hub (C9).** `nav-tasks` sits between Campaign Hub and Data Management; `#view-tasks` panel; `switchView` title `"TaskHub"`; `renderApp` branch; body class `module-tasks` applied by the existing `updateThemeColors()`.
- **C10 cyan tokens** added verbatim to `style.css` after the Data Management block, plus a new `✅ TASKHUB ROW STATE` block holding `.taskhub-row-overdue` / `.taskhub-row-today` / `.taskhub-row-done` / `.taskhub-orphan-chip` / `.taskhub-sortable`, each with a `.light-theme` override.
- **Filter strip** — All Open (default) · Past Due · Due Today · Upcoming · Date Range · Completed, using the same `.media-status-filter` component as audience status. Date Range reveals two date inputs. Changing any filter clears the selection, the sticky set, and returns to page 1.
- **Table** — ☐ · Due Date · First Name · Last Name · Company · Task Title. Names and company are looked up from `prospectId` at render (C1: never stored); an unresolved id renders `(missing prospect)`. All five columns sort both directions with a ▲/▼ marker; default due date ascending. Row color: overdue red, due today green, completed dimmed + struck through (completion wins over overdue). Pagination 25/50 with prev/next and a summary line.
- **Row click opens the 1.4 editor** (`openTaskEditor(taskId)`); the checkbox is excluded from the row click. TaskHub's **+ New Task** calls `openTaskEditor(null, null)` — the one surviving case that reveals the prospect picker.
- **Scope §12.2 shipped: Missing Prospect chip + resolution window.** Chip renders only above zero. It opens `#modal-task-orphans`, one row per orphan showing due date, title and the stored `prospectId` verbatim, each resolving two ways — assign a contact, or delete with a confirm. Acted on in place; the window closes and the chip disappears when the last orphan is resolved.
- **The editor's `isOrphan` repair branch is DELETED** per §12.1. `needsPicker` is now `isNew && !prospectId` only. `#task-orphan-warning` was removed from `index.html` too. An orphan opened from a TaskHub row shows `(missing prospect)` as fixed text; the hidden select still carries the unresolved id, so **saving preserves it rather than blanking it** — verified.
- **`refreshAfterTaskChange()` gained the TaskHub branch** (the 1.4 carry-in) — without it an edit made from TaskHub saves but does not repaint.
- **Scope §5 sticky completion:** a task completed while the active filter excludes completed tasks stays visible, struck through and dimmed, until the filter changes. Module-scope `taskStickyCompletedIds`, never persisted.
- `CACHE_NAME` bumped **`v69` → `v70`**, then **`v70` → `v71` → `v72`**. The second and third are Michael's review pass: the orphan window's Resolve cell was `display:flex` on the `<td>` itself, which drops table-cell behaviour so the row borders stopped lining up under that column. The controls now sit in a flex `<div>` inside the cell. v72 tinted the native date-input calendar glyph, which was invisible in dark mode.

## Files changed

**Modified:** `app.js` (`switchView` titles + subtitles, `renderApp` branch, `openTaskEditor` picker test + two stale comments, `saveTaskFromEditor` sticky hook, `refreshAfterTaskChange`, new `✅ RENDER VIEW: TASKHUB` section, seven listeners in `setupEventListeners`), `index.html` (nav button, `#view-tasks` panel, `#modal-task-orphans`, removed `#task-orphan-warning`), `style.css` (C10 + row-state block), `sw.js` (`CACHE_NAME`).
**Untouched:** no frozen contract modified. Nothing built outside UI. Session 1.6's bulk action bar was not started — `taskSelectedIds` is tracked and displayed in the summary, and nothing consumes it.

## Backup coverage (DIRECTIVES §4)

**No new store of user-writable data.** This session is a view over `state.tasks`, which Session 1.3 created and covered: it exports as `prm_tasks.csv` in the ZIP bundle, restores through all three `processRestoreFile()` paths, and rides inside the state JSON every local snapshot writes. The two writes this session adds — the orphan window's assign and delete — mutate `state.tasks[].prospectId` and the array itself, both already covered. `taskSelectedIds`, `taskPage`, `taskFilter`, `taskSort` and `taskStickyCompletedIds` are module-scope UI state and deliberately **not persisted and not backed up**; they are recomputed on every load. Nothing here is uncovered.

## Assumptions made

- **Nav position: between Campaign Hub and Data Management.** Scope §5 says "sixth hub alongside…" and does not fix the order. TaskHub is a working hub and Data Management is the utility, so it goes above it. Reversible; one line in `index.html`.
- **Date Range filters OPEN tasks only,** consistent with its three date siblings (Past Due, Due Today, Upcoming), which are all open-only. Completed has its own chip. Reversible.
- **The checkbox handler repaints only the summary line, not the table.** Re-rendering on every tick detaches the row under the cursor and drops focus — and it was a real defect, caught in verification: three consecutive checkbox clicks registered as one. Module-scope `taskLastPageInfo` caches the page numbers so `renderTaskHubSummary()` can run alone. Ladder 1 and 2 both point this way.
- **`taskFilter`'s C8 declaration was written literally, `orphan` included in its comment,** but that value is unreachable — §12.2 moved Missing Prospect out of the strip. A comment under the declaration says so.
- **A 30-task demo set was written for Michael's review pass, not committed to the repo.** `demo-tasks.js` — every id prefixed `task-demo-`, two deliberate orphans, one task carrying a multi-paragraph note with commas and embedded quotes. It is a console snippet, not app code; the matching one-line removal filters on that prefix. If 1.6/1.7 want it as a standing fixture, that is a decision to make deliberately — it is scratch until then.
- Verification ran against **seed data** (Jane Smith, Alex Rivera, Sarah Chen, Marcus Vance) with 60 tasks seeded by script, in a headless Chromium driven by Playwright in the cloud sandbox against a copy of the repo — *not* Michael's production database and not his browser profile. Nothing was written to his data.

## Open items

- ~~**Needs my eyes (1): cyan against the other five.**~~ ~~**Needs my eyes (2): full-row red on overdue.**~~ **BOTH CLOSED 2026-08-29** — Michael reviewed the running app in both themes and approved: *"I like the colors."* C10's cyan stands, and **full-row coloring for overdue and due-today stands as built** — the alternative was scoping `.taskhub-row-overdue td` down to `.taskhub-row-overdue .taskhub-due`, and it was not taken. Do not re-raise either, and do not "correct" the row coloring toward the domain-expiry cell-only convention; that difference is now deliberate.
- **Carried forward from 1.2, unchanged:** three drill attachments in `snapshots/files/` (`file-drill12-1..3`), safe to delete once snapshot `2026-08-29_101616.json` ages out.
- **Still unverified from here:** whether the *production* profile has its own backup folder and a green chip. No session has checked it yet.
- **Backlog (found, not done — out of compartment):** `parseCSVRow()` (app.js ~8770) does not handle escaped `""` inside a quoted field. Carried from 1.3; look in 1.8. **New:** `var(--color-danger)` is used in `renderDomainsView()` but is defined nowhere in `style.css`, so the expired-domain highlight is silently dead. Media/Domain compartment; look in 1.8.
- Carried forward, untouched: compliance obligations (§0), telemetry deferred to Phase 2, sheet/CSV import at scale, `Start_Vantage.bat` retirement.
- **Closed, do not re-raise:** snapshot cadence, chip placement, AQ drawer inconsistency, the task editor's prospect picker (§12.1 — the repair branch is now gone, and the resolution window replaced it).

## Scope amendment — recorded 2026-08-29, after this session

Michael reviewed 1.5 the same day and raised eight items. **`ai/spec/taskhub-scope.md` now carries a §13** and `ai/phases/phase-1-taskhub.md` has contracts **C14–C17** and sessions **1.9 / 1.10 / 1.11**. Nothing was built for them — the plan-only decision was deliberate, per Rule 1.

- **§13.1** the orphan window becomes a list; the row opens the editor. Reverses §12.2's per-row Assign/Delete.
- **§13.2** a type-to-search contact field replaces the full-list `<select>` everywhere a prospect is picked in a task path — orphan repair *and* "+ New Task".
- **§13.5** the Status dropdown becomes a Mark-complete checkbox at the top of the editor. C1 unchanged.
- **§13.8 — raised big, resolved small.** Michael: *"if there is one user interface central to the whole Vantage operation, it is the prospect inspector."* It was briefly scoped as one hoisted, movable, resizable panel across three hubs, which **conflicted with Phase 2's full-screen detail view** (`prospect-detail-view-scope.md` §1). **Michael chose the full-screen view**, and a drafted Session 1.12 was deleted the same day. Phase 1 now builds only the link: close the editor, `switchView("prospects")`, `selectProspect(id)` — three existing calls, inside 1.9. Phase 2 changes the destination, not whether there is one, so the link survives.
  **Carried into Phase 2's intake, and written into that scope file:** reachable from Prospect Hub, TaskHub and **Campaign Hub's audience lists**, never Media Hub; **bounded by the sidebar in both its states**; and **the back arrow restores the origin, not just the origin view** — opened from a task's editor, back reopens that editor, via a remembered `{view, taskId}` and one `openTaskEditor(taskId)` call. Unsaved edits: save before navigating, and do not navigate if validation rejects. *(A first draft of this claimed the editor was necessarily lost and billed it as the cost of full-screen. Wrong — Michael caught it. Full-screen replaces what is on screen; it does not stop the return path restoring it.)* The sidebar requirement is free — `#app-layout` is `grid-template-columns: auto 1fr`, so anything inside `#main-canvas` already stops at the sidebar and reflows with its 76px/280px transition. The only way to break it is `position: fixed`. That note is in the scope file.
  **The two-inspector duplication stays open.** `renderInspector()` (245 lines / 24 ids) and `renderAqInspectorDrawer()` (190 / 16) still render the same records twice and have already drifted — 1.4's Tasks subsection is in one and not the other. Phase 2 replacing the Prospect Hub inspector is when that gets fixed or becomes permanent. BUILD_NOTES open risk 3 stands.
- **Also fixed during the review pass, v72:** Chrome's `::-webkit-calendar-picker-indicator` ships a near-black glyph that is invisible on the dark canvas — which is why §13.6's already-existing date picker was missed. Now masked and tinted with `var(--color-primary)`, so it follows the hub.
- **§13.3** TaskHub's header block freezes; only rows scroll.
- **§13.4** columns resize and reorder by drag, layout persisted and backed up. Built generic, keyed by table id; TaskHub wired first.
- **§13.6 closed with no work** — the due-date picker already exists; `<input type="date">` carries Chrome's native calendar. Do not re-raise.
- **Select-all-on-page** folded into 1.6 as a **header checkbox**, not the AQ-style buttons.

**The `isOrphan` branch this session deleted comes back in 1.9.** That is not a mistake being undone — §12.1's actual rule survives intact: the picker appears on `isOrphan` and on create-with-no-prospect, never on an ordinary edit.

**Phase 1 is now 11 sessions**, and **run order is not numeric order** — 1.8 is the phase close and moves to last. See the plan's Session order block for why it kept its number.

## Next step

Run **Session 1.6** (selection, bulk complete, history logging) from `ai/phases/phase-1-taskhub.md`, in a **new conversation**. 1.9 and 1.10 depend only on 1.5 and can run before it if the hub's usability matters more than bulk actions.

**1.6 shrank.** Three of its eight tasks landed in 1.5 and are struck through in the plan — the selection `Set`, the pure-selection row checkbox, and sticky completion. Check before rebuilding them.

Carry in: `taskSelectedIds` already exists, is maintained by the row checkboxes, survives page turns within one filter run, and clears on any filter change — 1.6 adds the bulk action bar that consumes it, not the Set. `renderTaskHubSummary()` already reports the count; the action bar can key off `taskSelectedIds.size` the same way. `taskStickyCompletedIds` and `markTaskStickyCompleted(id)` exist and are already called from `saveTaskFromEditor()` — **`bulkCompleteTasks(ids)` must call `markTaskStickyCompleted()` per task too**, or bulk-completed rows will vanish under the cursor, which is the exact thing scope §5 forbids. Contract C5 is the history entry shape and C6 registers `"Task Completed"` as a reachout type in **both** places (first-run literal and the `ensureStateDefaults()` push) — one without the other silently drops the type on restore. 1.6 should also decide whether to backfill history for tasks completed in the editor between 1.4 and now (AIContext 1.4 flagged this).
