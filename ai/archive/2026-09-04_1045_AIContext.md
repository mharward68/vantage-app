# AI Context — PHASE 3 IN FLIGHT, SESSION 3.3 SHIPPED

**Updated:** 2026-09-04 10:21 (America/New_York).

**Last run:** Phase 3 / **Session 3.3 — email launch: URL builders, buttons, converter, clipboard, guards.** Compartment: **UI + LOGIC**. ✅ **COMPLETE — and it produced a new frozen-contract amendment, A4.**

**State:** `app.js` **18,383** (+510) · `index.html` **3,920** (+44) · `style.css` **5,067** (+76). `CACHE_NAME` **v129 → v131**, **TWO bumps** (v130 built the session; v131 landed A4). **One-glance version tell:** console `gmailBase()` ends in `/mail/` on v131 and in `/mail/u/<address>/` on v130. `typeof gmailSearchUrl` is `"undefined"` on both and must stay that way.

**Git: ⛔ NOT COMMITTED. Four code files are dirty on `C:` — `app.js`, `index.html`, `style.css`, `sw.js`** — plus `ai/phases/phase-3-outreach-launch.md` (A4) and the three document files this close writes. **Michael runs git.** A parent SHA is NOT carried here: no ref was read this session, and BUILD_NOTES says a session that cannot verify repo state writes UNKNOWN rather than a number.

**Database: 1,561,640 → 1,561,663 bytes, and the +23 is fully accounted for.** It is exactly the 23-character work Gmail address written into a previously empty string, **predicted before it was measured**. 651 prospects · **0 tasks** · taskSettings still **2 keys** (`dateMode`, `workGmailAddress`). `state.activeView` returned to `dashboard`, the view the session opened on. Only `vantage_prm_database` and `vantage_sidebar_pinned` in `localStorage`.

---

## ⛔ AMENDMENT A4 — the headline, and the thing a later session must not undo

**Contract Q4's `gmailBase()` returns "Temporary Error (404)" from real Gmail.** Putting the address in the path — `/mail/u/<address>/` — does not work. Michael authorised the fix in-session; it is written into `ai/phases/phase-3-outreach-launch.md` § Frozen-contract amendments as **A4**, with the four-navigation evidence table.

```js
function gmailBase() { return `https://mail.google.com/mail/`; }
// gmailComposeUrl(): ?authuser=<workGmailAddress>&to=…&su=…&body=…&tf=cm
```

- ⛔ **NOT a licence to fall back to `/u/0/`.** Q4's reasoning stands: `/u/0/` reflects sign-in order and renumbers silently when a Google account is added. `authuser=` names the account and lets Google resolve the index — Q4's intent, different address bar.
- **It degrades better.** With the account not signed in, `authuser=` lands on Google's sign-in page **carrying the compose payload in `continue=`**; the frozen path form was a 404 dead end with the typed message gone.
- **`encodeURIComponent` is innocent** — the literal `@` 404s too. `%40` is fine in the query string.

## What was done — all four files

| # | Edit |
| --- | --- |
| 1 | `app.js § ✅ TASK EDITOR — OUTREACH LAUNCH`, a new sub-block before `closeTaskEditor()`: the converter (`OUTREACH_MD_RE`, `outreachConvertBody`, `outreachBodyToHtml`, `outreachFlattenBody`, `outreachBodyHasLink/HasFormatting`, `outreachClipboardPayload`, `outreachCountedLength`), the builders (`gmailWorkAddress`, `gmailBase`, `gmailComposeUrl`, `GMAIL_URL_MAX`, `gmailComposePlan`), the clipboard helper (`outreachCopy`), the toast (`showOutreachToast`, `outreachCopyToast`), and the controls (`taskOutreachDraft`, `outreachActionButton`, `outreachHint`, `handleTaskLaunchGmail`, `handleTaskCopyAddress`, `handleTaskCopyMessage`, `renderTaskOutreachActions`). |
| 2 | `syncTaskMsgCount()` gained a `channel` argument — **A2: LinkedIn counts the FLATTENED output.** `syncTaskOutreachActions()` added; `syncTaskOutreachBlock()` now repaints the actions. |
| 3 | Two new listeners in `setupEventListeners()` — `#task-msg-to` and `#task-msg-body` `input` → `syncTaskOutreachActions` (To decides a disabled button; the body decides whether the URL degrades). Deliberately **not** routed through `syncTaskOutreachBlock()`. |
| 4 | **Settings field** `#setting-outreach-work-gmail`, its `change` listener and its render leg in `renderSettingsLists()`. **ONE key, not two — A1.** |
| 5 | `index.html`: empty `#task-outreach-actions` under the Message group, and the Outreach Work Gmail section under the TaskHub settings heading. |
| 6 | `style.css`: `✅ TASK EDITOR — OUTREACH LAUNCH CONTROLS` block after the 3.2 block (`🧱 HUB SHELL` still last — S5), plus `.outreach-toast`. |
| 7 | `CACHE_NAME` v129 → v130 → v131. |

**There was no toast facility in this codebase before this session.** `showOutreachToast()` is new, deliberately minimal — one node, one 5-second timer, no queue, no store — because the copy paths need a visible success state and it cannot be `alert()`, which blocks and would fire straight after a `window.open`.

## Verification — all of it ran

- **Decode-back, on v131:** parameter order `authuser, to, su, body, tf`; all four `decodeURIComponent` back **character-identical** to `state` on a body with two blank lines, `&`, `+` and `"`. **7 newlines → 7 `%0A` → 7 newlines.** `tf=cm` present, `view=cm` / `fs=` **absent**, `/bcc/i` **absent**, no address in the path.
- **A1:** `"emailBcc" in state.taskSettings` → `false`; one `"Outreach Work Gmail"` settings row, **zero** Bcc rows; the three `&bcc=` hits in `app.js` are all comments forbidding it.
- **A3:** `typeof gmailSearchUrl === "undefined"`, `grep -c 'function gmailSearchUrl'` → **0**; a `thread` task renders **no launch button** and two numbered copy buttons. **Both clicked for real:** `1 · Copy address` then a real Ctrl+V pasted the 23-char address; `2 · Copy message` then a real Ctrl+V pasted the flattened body — **the address was gone**, which is the sequence made visible.
- **A2, end to end in real Gmail (the only evidence that counts):** rich write attempted with `["text/html","text/plain"]`; a real Ctrl+V into a compose window produced **1 anchor, text "Tech RFP", href on the probe host, URL not visible as text, 1 `<strong>`, 1 `<em>`, no raw markdown.** Gmail honours the HTML flavour from `localhost`. Toast reads *"Message copied with link."*
- **The fallback, exercised not assumed:** `clipboard.write` forced to reject → fell to `writeText` with the **flattened** text carrying the **bare URL**, no markdown, and the toast changed to *"Message copied — plain text."*
- **LinkedIn:** payload is `text` only, `html === null`, equal to the flattened form, **not** the raw stored string, no markdown.
- **Counters follow the output (A2):** stored 303 / 304 / 305 read **299/300 · 300/300 · 301/300**, red only at 301.
- **Popup-blocker check, the one a normal check cannot see:** inside one real click, synchronous `window.open` → **Window**; the same call after an await outliving user activation → **null**.
- **Guards:** blank address → disabled + the Settings message; blank To → disabled + its own message; a body with a link **and** a >2000-char body each drop `body=` and show the degrade notice **before** the click.
- **One real open per Assumption 8, by the shipped button:** compose window with To, Subject carrying `"quoted" & +`, and four paragraphs with the blank lines intact.
- **Clean five-line console, zero errors or warnings across 42 messages, no favicon 404.** All six views at 3.1b's numbers — dashboard 89 · prospects 6,684 · media 1,148 · campaigns 272 · tasks 49 · **data-management 68 measured first on a fresh boot**, and it contains nothing this session added.
- **`node --check` parses and `check_ids.py` is at baseline** (`{export-backup-btn, restore-backup-input}`), both run against the files **as they sit on the mini-PC**, md5-matched to the bytes the browser ran.

## Backup coverage — DIRECTIVES §4

**NO STORE WAS ADDED OR MODIFIED, AND THAT IS THE WHOLE ANSWER.** The Settings field writes `state.taskSettings.workGmailAddress`, which Session 3.1 already seeds, exports as the `["Outreach Work Gmail", …]` row and restores. **No new top-level store, no new CSV file, no new `wipeAllData()` line, `TASKS_CSV_HEADERS.length` still 18, and contract Q8 is untouched.** The toast, the launch controls and the disclosure of which rung the clipboard used persist **nowhere**. Nothing this session added is user-writable data.

## Assumptions logged this session

1. **The buttons read the EDITOR, not the saved record** (`taskOutreachDraft()` → `readTaskOutreachFromEditor()`). Launching what is on screen avoids a save-then-launch dance, and nothing in the path writes to state — Cancel still means Cancel. Reversible.
2. **The counter change (A2's LinkedIn flattening) landed here, not in 3.2 or 3.3c.** It cannot be done before the converter exists and the converter is this session's. One argument added to `syncTaskMsgCount()`.
3. **`gmailBase()`/`gmailWorkAddress()` read `state.taskSettings` defensively** (`&& … || ""`) rather than the bare dereference Q4 writes literally. Same output, no throw on a malformed record.
4. **LinkedIn's converter path was proved through `outreachClipboardPayload()`, not through a button** — the LinkedIn controls are 3.4's. The function 3.4 will call is the one that was verified.
5. **The real open was confirmed against the account signed into the verifying browser profile**, because the work account is not signed in there. The mechanism is proved; the inbox is not.

## Open items

- ⛔ **COMMIT** `app.js`, `index.html`, `style.css`, `sw.js`, `ai/phases/phase-3-outreach-launch.md`, `ai/AIContext.md`, `ai/archive/2026-09-04_1021_AIContext.md`, `ai/BUILD_NOTES.md`. One commit. **Michael runs git.**
- ⛔ **`michaelh@youravdept.com` IS NOT SIGNED IN TO THE CHROME PROFILE THAT VERIFIED THIS.** Only the personal account is; `/mail/u/1/` redirects to a sign-in page. **The button will land on Google's sign-in page (payload intact) until he signs in.** 3.4 or the 3.5 close owes the one confirmation that it reaches the work inbox.
- **Two stray Gmail drafts** in the personal account from verification — *"Vantage 3.3 verification — do not send"* and *"A2 rich paste probe"*. Left in place; deleting mail is not a session's job.
- **Next is 3.3c — the authoring surface** (UI, M, ~8 min, Confidence High): the Bold/Italic/Link toolbar, the two disclosures, the live preview **rendered by 3.3's `outreachBodyToHtml()`, not a second renderer.** Then **3.4** (LinkedIn — `?recipient=` is undocumented, verify live, ship the profile-URL fallback the same session; **Premium confirmed, so `connect` is BUILT**), then the **3.4 review point**, then **3.5**.
- **Phase 2B is still not formally closed** — its snapshot re-verify has not run.
- **Phase 2C, scoped not built:** the sixteen-store `wipeAllData()` gap and the `ensureStateDefaults()` `length === 0` reseed defect.
- **The snapshot system is alive** — every boot read a snapshot off disk, and one `[Snapshot] Confirmed … (visibilitychange)` fired mid-session with a prune to 15 kept.
- **Still owed by Michael, now a SEVENTH close carrying them:** the five `DECISIONS.md` DECLARATIONS amendments; the two from 2A.6; the domain-is-identity amendment; the 2B.16/2B.17 P4/P5 divergence amendments. `LA` = Louisiana or Los Angeles. Finding 10d's meaning. Three cosmetics from 2B.4 — **leaving them is a valid answer and saying so closes them.** ⚠️ **A4 also owes a `DECISIONS.md` entry** — the reasoning is in the plan; the standing files have not been updated.
- **`ai/phases/phase-2b-RUNSHEET.md` is spent and marked for deletion. Michael deletes it.**
- **`DECLARATIONS.md` Stack line counts are stale** — says `app.js` ~13,270; real is **18,383**. Propose at the 3.5 close; do not edit mid-phase.
- **Phase backlog, carried:** the `"Note"` reachout-type reassignment gap; ProspectHub OR-s tags while Advanced Query AND-s them; `p.notes` / `p.location` shape defaults; company-edit uniqueness guard; the §4 back-fill of existing `"domain.com"` rows; the missing `<link rel="icon">`. **The repo is PUBLIC** and DIRECTIVES §0 compliance is undecided. Stale `..\backups\`.
- **Both query surfaces stay DEFERRED**; the `sequences` tab stays `enabled: false`. Neither was touched.
- **The enrollment compartment still has no scope.** Phase 3 needs a Prompt 1 intake before 3.6+ can exist. Fifth close to say so.

## Estimate vs actual

Sized **M / ~10 min / Medium**, and **the size was right while the confidence was the thing that earned its keep** — Medium was assigned because "this session owns every mechanism in the phase that fails invisibly", and the session's one real surprise was exactly that: a frozen contract that passes every console check and 404s in the browser. **Two `CACHE_NAME` bumps against a budget of two.** **Michael's time was ~4 minutes** across three round trips — the boot answers, one clipboard aside, and one genuine frozen-contract decision, which is the one that had to interrupt him.

## Files changed

**Code:** `app.js`, `index.html`, `style.css`, `sw.js`. **Documents:** `ai/phases/phase-3-outreach-launch.md` (amendment A4), `ai/AIContext.md`, `ai/archive/2026-09-04_1021_AIContext.md` (new), `ai/BUILD_NOTES.md`.

## ⛔ EXACT NEXT STEP — **SESSION 3.3c**

Commit the eight files, then run **3.3c — the authoring surface** from `ai/phases/phase-3-outreach-launch.md`. **Read A1, A2, A3 AND NOW A4 before trusting Q1–Q7.** 3.3c writes no converter and no clipboard code: it is a toolbar, two disclosures and a preview over `outreachBodyToHtml()`, and **the preview and the clipboard must come from that one function** — that is what makes "what you see is what pastes" structural instead of tested.

**Carry forward:** ⛔ **`device_commit_files` RE-SENT THE PREVIOUS VERSION WHEN THE SAME STAGED PATH WAS REUSED** — it reported `written`, the device mtime advanced, and the bytes were the OLD ones. Cost one wrong diagnosis. **Write each revision to a NEW staged path and confirm the device file SIZE with `device_list_dir` before believing a commit.** ⚠️ **`javascript_tool` RETURNS `[BLOCKED: Cookie/query string data]`** for a raw URL or HTML string — split on `/[?&]/` and swap `=` for an arrow. ⚠️ **`navigator.clipboard.readText()` HANGS the tool for 45s** — spy on the write call or paste with a real Ctrl+V instead. ⚠️ **THE SCREENSHOT SCALE CHANGED MID-SESSION** (1456-wide → 952-wide) because the window was resized — re-derive it from a `getBoundingClientRect()` every time. **THE APP RUNS PERFECTLY WITH THE SERVER DOWN** — three-probe first; it read `200 / 404 / 200` this session. **`state.activeView` is PERSISTED and moves the byte count.** **Measure `data-management` on a fresh boot.** `state` is not `window.state`. **Do not open the app while Michael's window is up.** **TEST AGAINST REAL RECORDS, WRITE UP PLACEHOLDERS — the repo is public.**
