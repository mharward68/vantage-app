# AI Context

**Updated:** 2026-08-29 14:24 (America/New_York)
**Last run:** Phase 1 / Session 1.7 — Bulk due-date editor, business-day arithmetic, global setting  **Compartment(s):** LOGIC
**State:** `node --check app.js` clean · `check_ids.py` at its known baseline of two · console errors identical to the unmodified tree · `CACHE_NAME` v75 · deployed n/a
**Estimate vs actual:** planned **M** / ~5 min of Michael's time. Actual **M**, and ~**10 min** of his — ~2 min on the session, the rest on a wrong-folder detour (see Assumptions). No inputs needed and none asked. No three-strikes situation; nothing escalated; no frozen contract touched.

## Done

- **`shiftTaskDate(dateStr, n, mode)` per C11**, verified against all three frozen vectors. Steps day by day from the given date **without normalizing the start**, counting Mon–Fri when `mode === "business"` and every day when `"all"`. The arithmetic runs in **UTC** — see BUILD_NOTES; a local-time `setDate()` loses a step across a DST boundary.
- **One modal, `#modal-task-due-date`, two modes.** Mode A shift by N days with a `[− / +]` direction select and a number input; mode B set to a specific date, **honored exactly as entered, weekend or not** — nothing snaps, nothing warns. Radios switch the visible group and the counting note; nothing commits until Apply.
- **Edit Due Date is a button in the existing `#taskhub-bulk-actions` bar**, next to Mark Complete — not a second bar, per the plan's Next step for this session.
- **Completed tasks in the selection are skipped and the count is reported**, in three places: the modal's live summary line, the confirm, and the result alert. `splitTaskSelectionForDueDate()` is the shared splitter, the same shape `bulkCompleteTasks()` uses.
- **Global `dateMode` toggle** is a `<select>` in the existing **⚙️ Application Settings** modal under a new cyan **TaskHub** group, reached from a new `#btn-tasks-settings` gear in the TaskHub header — the same control every other hub already has. Writing it is **never retroactive**; verified by pasting every `dueDate` before and after the change.
- **C2 and C4 needed no work.** Session 1.3 already landed `state.taskSettings = { dateMode: "business" }` in `ensureStateDefaults()` (app.js 1262) and the `["Task Date Mode", value]` row in `generateSettingsCSV()` / `restoreSettingsFromCSV()`. Verified, not assumed.
- `CACHE_NAME` bumped **v74 → v75**.

## Files changed

**Modified:** `app.js` (new "Bulk due-date editor" sub-block inside `§ ✅ RENDER VIEW: TASKHUB`, immediately after `clearTaskSelection()`: `shiftTaskDate` / `taskDateMode` / `splitTaskSelectionForDueDate` / `openBulkDueDateModal` / `closeBulkDueDateModal` / `syncBulkDueDateModal` / `applyBulkDueDate`; section 13 at the end of `renderSettingsLists()`; seven listeners in `setupEventListeners`), `index.html` (`#btn-taskhub-bulk-duedate` in the bulk bar, `#btn-tasks-settings` in the TaskHub header, the `#modal-task-due-date` modal, the TaskHub group in `#modal-media-settings`), `style.css` (`✅ BULK DUE DATE MODAL` block — a radio-button override, see below), `sw.js` (`CACHE_NAME`).
**Untouched:** everything outside LOGIC. No frozen contract modified.

## Backup coverage (DIRECTIVES §4)

**No new store of user-writable data.** This session writes to two already-covered things. `state.tasks[].dueDate` is covered by Session 1.3 — the `Due Date` column of `prm_tasks.csv`, in the ZIP and in all three `processRestoreFile()` paths, plus the state JSON in every snapshot. `state.taskSettings.dateMode` is covered by **contract C4** — the `["Task Date Mode", value]` row in `prm_settings.csv`, written by 1.3 and confirmed present this session. **No export or restore code was added, deliberately** — per BUILD_NOTES, a feature that only mutates already-covered fields needs none. Nothing here is uncovered.

## Assumptions made

- **The `dateMode` toggle went into Application Settings, not the TaskHub header.** DIRECTIVES §5: all gates hold either way (reversible, no data-model change), so the ladder decides, and options first differ at rung 2 (UX quality). `#modal-media-settings` is titled "⚙️ Application Settings" and is where every configurable option in the app lives, reached from a ⚙️ button in each hub's header; putting TaskHub's only global setting anywhere else means the person who looks in the obvious place does not find it. It is also the first **non-list** section in that modal — every existing one is an item list plus an inline add. Reversible.
- **A bulk due-date change clears the selection**, matching Session 1.6's convention for Mark Complete. Re-shifting the same rows a second time is not the normal intent, and a shift can move a row out of the active filter anyway. Reversible: one line.
- **A task whose date does not actually move is reported, not silently counted.** Setting five tasks to a date two of them already had reports "3 … 2 already had that date and did not change." The alternative — counting them as moved — is the small dishonesty class this phase keeps designing against.
- **Verification ran in headless Chromium via Playwright in the cloud sandbox, against a copy of the repo** with scripted tasks against the seed prospects — **not** Michael's production database and not his browser profile. Nothing was written to his data.
- **Detour, not an assumption, but it cost the time:** the folder connected at the start of this session was `E:\01_AppDevelopment\…\vantage-app`, a **stale clone** — last commit 2026-07-23, no `ai/` folder, still carrying the retired `AI-CONTEXT.md` / `StatementOfDirective.md`. The GitHub remote `mharward68/vantage-app` is also stale: its last commit is `a1e4a34`, 2026-08-28, Phase 0 close, with an **empty `ai/phases/`**. The session halted and asked rather than guessing; Michael connected `C:\…\vantage-app`, which is the live tree. **See Open items — the E: copy is a live hazard.**

## Open items

- **Two stale copies of this repo exist and both look real.** `E:\01_AppDevelopment\02_Vantage-Master-Folder\vantage-app` is six weeks behind and follows the retired context protocol; a session pointed at it would boot on documents that were deleted in Phase 0. The GitHub remote is at Phase 0 close — **nothing from Phase 1 has been pushed**, so sessions 1.1–1.7 exist only in the working tree on `C:`. That is also the only copy of `ai/phases/phase-1-taskhub.md`. **Needs Michael's eyes:** delete or clearly mark the `E:` copy, and push `C:` to the remote — right now a disk failure loses the entire phase, and the Tier 1 snapshots protect the database, not the source.
- **Carried forward from 1.2, unchanged:** three drill attachments in `snapshots/files/` (`file-drill12-1..3`), safe to delete once snapshot `2026-08-29_101616.json` ages out.
- **Still unverified from here:** whether the *production* profile has its own backup folder and a green chip. No session has checked it yet.
- **Backlog (found, not done — out of compartment):** `parseCSVRow()` (app.js ~8770) does not handle escaped `""` inside a quoted field — carried from 1.3, look in 1.8. `var(--color-danger)` is used in `renderDomainsView()` but defined nowhere in `style.css`, so the expired-domain highlight is silently dead — Media/Domain compartment, look in 1.8. **Nothing new was found this session.**
- Carried forward, untouched: compliance obligations (§0), telemetry deferred to Phase 2, sheet/CSV import at scale, `Start_Vantage.bat` retirement, the two-inspector duplication (`renderInspector` vs `renderAqInspectorDrawer` — BUILD_NOTES open risk 3, resolved or made permanent in Phase 2).
- **Closed, do not re-raise:** the history backfill (scope §14.3, closed permanently with no work); TaskHub's cyan and its full-row overdue/due-today coloring; snapshot cadence; chip placement; AQ drawer inconsistency; the task editor's prospect picker; §13.6's date picker.

## Next step

Run **Session 1.9** (orphan window becomes a list; contact search replaces every prospect dropdown; Mark-complete checkbox; prospect link) from `ai/phases/phase-1-taskhub.md`, in a **new conversation**. It depends only on 1.5, and contracts **C14–C17** plus scope **§13** and **§14.4** govern it. 1.10 is equally available and equally unblocked; **1.11 must follow 1.10**; **1.8 is the phase close and is always last.**

**Read scope §14 before any session that touches completion, prospect history, or `getLastReachoutDate()`** — §8 says the opposite and is superseded. Session 1.9 owns §14.4 (transposition): a branch **inside** `logTaskCompletionHistory()`, never a second history writer, and the safe default is single-completion only.

Carry into 1.9: `shiftTaskDate()` is now the one place task dates are computed — if 1.9 or any later session needs date arithmetic, call it rather than re-deriving. `state.taskSettings.dateMode` governs **counting only** and must never be applied retroactively.
