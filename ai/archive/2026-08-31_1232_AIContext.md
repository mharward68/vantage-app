# AI Context

**Updated:** 2026-08-31 11:45 (America/New_York)
**Last run:** Phase 2B / **Session 2B.2** — column-layout machinery generalised, proven against TaskHub  **Compartment:** UI
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two (`{'export-backup-btn', 'restore-backup-input'}`) · `CACHE_NAME` **v84** · `app.js` 13,234 / `index.html` 3,219 (untouched) / `style.css` 3,148 (untouched) · deployed n/a
**Estimate vs actual:** sized **M / ~10 min / High confidence**; ran **M**, ~**6 min** of Michael's time (two questions up front, one window-hygiene warning, one review). One `CACHE_NAME` bump, not the budgeted two — do not record that as a win until the review pass.
**One-glance version tell:** TaskHub still resizes and reorders exactly as before, and `COLUMN_TABLES` exists in the console. If `typeof COLUMN_TABLES === "undefined"`, you are on the old cached document — **reload once more.**

## What was done

P7's registry and its six table-id-parameterised functions exist. `taskhub` is their only consumer; **no ProspectHub table was touched**, and `PROSPECTS_COLUMNS` / `COMPANIES_COLUMNS` were deliberately not written.

- `COLUMN_TABLES` (one entry) + `columnTable(tableId)` guard, immediately after `TASKHUB_COLUMNS`.
- Six renames per P7: `layoutRecord` / `layoutColumns` / `layoutColumnWidth` / `setLayoutColumnWidth` / `setLayoutColumnOrder` / `initHeaderDrag`, all taking a table id. The C15 read rule moved **verbatim** — unknown keys ignored on read and never deleted, `0`/absent width → code default.
- Helpers generalised with them: `layoutHeaderCell` / `layoutResizeTarget` / `layoutResizeHitCell` / `layoutHeaderCells` / `layoutSwapAdjacentColumnCells` / the drop line. New `ensureLayoutRecord()` (the four lines both writers used to inline) and `layoutLeadingCellCount()`.
- Constants renamed `TASKHUB_*` → `LAYOUT_*`; **values unchanged**. `taskHeaderDragInit` (boolean) → `layoutDragInit` (object keyed by table id).
- `ensureStateDefaults()`'s default record now loops `COLUMN_TABLES` instead of hardcoding TaskHub's five keys. Existing keys are left untouched, so a restore is never overwritten at boot.
- `<tbody>` and the scroll wrapper are **derived** from `tableId`, not listed in the registry — P7 lists four fields and the registry still has exactly those four.

## Files changed

`app.js` (13,124 → 13,234), `sw.js` (v83 → **v84**), `ai/BUILD_NOTES.md` (MAP entry rewritten to the new names; two new notes), `ai/AIContext.md`. **`index.html` and `style.css` were not touched** — the plan listed `index.html` as modified, and it turned out nothing there needed to change.

## Assumptions — all reversible, all logged

1. **Ran ahead of Phase 2A, on Michael's explicit go-ahead.** 2A has *not* shipped (verified: the three viewport constants are live, hub labels unchanged, no 2A plan or handoff). The plan's blocking banner is written against 2B.1's back arrow and 2A's scroll shape; 2B.2 touches neither, and the plan itself calls it "independent of 2B.1" and "genuinely parallelizable." **The 2B.1 → 2B.6 chain is still blocked on 2A.**
2. **No delegate shims.** The plan's fallback offered keeping the six `taskHub*` names as one-line delegates. Not taken — every call site was updated instead, per the no-workarounds standard. The revert path is `git checkout app.js`, which is no worse.
3. **Constants and helpers renamed to the `LAYOUT_*` / `layout*` prefix** even though P7 names only the six. They are gesture geometry and table plumbing, not TaskHub's, and a half-renamed module is what makes 2B.8 expensive.
4. **The drop-line element keeps the id `#taskhub-drop-line`** and the drag still applies the `taskhub-col-*` classes. One element serves every table; renaming would be a CSS change in a session whose point is changing nothing observable. **2B.8 widens the CSS.**
5. **`layoutLeadingCellCount()` measures the offset** rather than assuming TaskHub's leading checkbox column. Returns 1 for TaskHub, 0 for a table without one.

## Open items

- **Carried, unchanged:** `state.taskSettings` still missing from `wipeAllData()` — **re-confirmed with fresh evidence today**: `dateMode: "business"` survived a real wipe in this session's drill. Session **2B.7** owns it. `--color-danger` still undefined, six call sites, Session **2B.3**.
- **Phase 2A is still not run.** It blocks 2B.1 and everything downstream of it. 2B.8 (the second consumer of this session's work) is blocked on 2B.6, which is blocked on 2A.
- **2B.8 inherits three TaskHub-shaped edges**, all recorded in `BUILD_NOTES.md`: the CSS class names, the `.table-scroll-container` derivation, and the shared drop-line id.
- Unchanged from Phase 1: two Vantage windows overwrite each other (one window at a time); `parseCSVRow()` `""` gap; repo is PUBLIC; DIRECTIVES §0 compliance undecided; stale `..\backups\`; `schema_update.sql` still deletable.
- **Needs Michael's eyes:** nothing new. Confirm TaskHub still *feels* the same — that is the outcome this session existed to protect. Also still carried: the 6px resize zone and divider weights, the 20-row search cap and 220px results max-height, the `dueDate: 0` code-default-124 assumption and the five default widths.

## Backup coverage — DIRECTIVES §4

**No new store, no new field, no new CSV column, no migration.** `state.columnLayouts` is unchanged in shape and keeps its entire existing coverage: the one `["Column Layouts", <json>]` row in `prm_settings.csv` (C17). Proved live today, character-identical across export → `wipeAllData()` → restore on real data (4 prospects / 5 companies / 31 tasks / 30 media / 1 audience list, all counts identical after). The wipe genuinely cleared `state.columnLayouts` to `{}` first, so the restore leg could have failed.

## Next step

1. **Review TaskHub by hand** — resize a column, reorder one, reload. That is the regression this session exists to protect and no state check substitutes for it. Expect a second `CACHE_NAME` bump if the review turns anything up.
2. **Commit** — `app.js`, `sw.js`, `ai/BUILD_NOTES.md`, `ai/AIContext.md`, and the archived context file.
3. **Then Phase 2A.** Run `ai/phases/phase-2a-RUNSHEET.md` end to end — plan, sessions, close. **2B.1 cannot start until it closes**, and every remaining 2B session except 2B.9 sits behind 2B.1.

**Carry forward:** grepping for any `taskHub*` layout name now returns nothing — that is Session 2B.2, not a missing file. `state` is not `window.state`. A synthesized drag does not fire the trailing click; dispatch it yourself or the suppress-flag test lies. One Vantage window at a time.
