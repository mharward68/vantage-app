# AI Context

**Updated:** 2026-08-29 09:52 (America/New_York)
**Last run:** Phase 1 / Session 1.1 — Snapshot writer, health state, and restore  **Compartment(s):** DATA
**State:** builds ✅ · app loads at `localhost:5000` with a clean console (5 boot messages, no errors) · deployed n/a
**Estimate vs actual:** planned **L / ~15 min of Michael's time**. Actual ≈ **L**, and ~**10 min** of his time — four short interactions (pick the backup folder, one tab switch, one folder rename and rename-back). Two mid-session bug fixes are what kept it at L rather than dropping to M.

## Done

- Debounced snapshot writer (~2 min after last mutation, fired from `saveState()`), plus `visibilitychange`→hidden and a guarded `beforeunload`. Writes to `<backup folder>/snapshots/` per C12.
- **Read-back confirmation.** `lastConfirmedAt` is stamped in exactly one place, only after the file is re-opened and found non-zero. `close()` resolving is never treated as evidence.
- `state.snapshotHealth` per C13, stripped from every backup (`exportJSONBackup`, snapshot payload) and recomputed from the directory on boot rather than trusted from cache.
- Watchdog on boot + every 60 s resolving red / amber / green from the C13 table.
- Sidebar health chip, visible from every hub, in all three states; clicking it re-grants folder permission and forces an immediate snapshot. One-shot red modal on the first interaction after entering red, once per session.
- Sticky `failed` — cleared only by a read-back-confirmed write. **No download fallback anywhere:** the snapshot path never calls `saveBackupFile()` and never reads `backupFolderSessionDisabled`.
- Data Management: snapshot status line, snapshot count, dated snapshot picker + Restore, and a Snapshot Now button. Restore routes through the shared `applyJSONBackupText()` engine, factored out of `restoreJSONBackup()`.
- `CACHE_NAME` bumped `v59` → **`v62`** (three bumps: initial build, then each of the two fixes below).

## Files changed

**Modified:** `app.js` (new 📸 LOCAL SNAPSHOTS section after the backup-folder block; `saveState()`, `ensureStateDefaults()`, `DOMContentLoaded`, `exportJSONBackup()`, `restoreJSONBackup()`, `renderDataManagementView()`, `setupEventListeners()`), `index.html` (sidebar chip; Data Management snapshot section), `style.css` (chip styles, dark + light), `sw.js` (`CACHE_NAME`).
**Untouched:** everything else.

## Assumptions made

- **C13's red condition is implemented as both readings, OR'd.** The contract text says "`lastMutationAt > lastConfirmedAt` by more than 5 minutes"; read literally, the required "clear the debounce and wait 6 minutes" drill would stay green. Implemented as *(difference > 5 min)* **OR** *(an unprotected mutation has sat for > 5 min)*. This can only over-report risk, never claim protection that isn't there. **Not a contract change** — it is what makes the contract's own Done-when observable. If it should read only one way, that is a plan revision.
- Snapshot restore uses an in-page dated `<select>` rather than `showOpenFilePicker`, so it needs no OS dialog and is callable as `restoreFromSnapshotFile(name)`. Simpler surface, and testable.
- Verification ran against **seed data** (4 seed prospects) in the Chrome profile the extension drives — *not* Michael's production database, which lives in a different profile. Agreed 2026-08-29. Gate C is satisfied on mechanism; the restore has not been exercised against production volume.

## Open items

- **Two zero-byte snapshot files remain on disk** (`..._093359.json`, `..._093529.json`) from before the `beforeunload` fix. Correctly ignored by `listSnapshotFiles()`, but they emit one warning per boot. **Backlog → Session 1.2's pruner should sweep zero-byte files.**
- **Needs Michael's eyes:** the red chip sits directly above Reset Sandbox / Wipe Data, which are also red. It may read as one more destructive button rather than a status. Screenshots sent in chat 2026-08-29.
- The backup folder handle is set **per browser profile**. It is set in the extension-driven profile; it is *not* set in whichever profile holds production data. Until it is set there, production is unprotected and the chip will show red there. **This is the first thing to do before 1.3.**
- Carried forward, untouched: compliance obligations (§0), telemetry deferred to Phase 2, sheet/CSV import at scale, `Start_Vantage.bat` retirement.

## Next step

Run **Session 1.2** (Snapshot retention and binary mirror) from `ai/phases/phase-1-taskhub.md`, in a **new conversation**.

Carry in: the pruner must also sweep zero-byte snapshots (above); `snapshots/files/` is never touched by the pruner; a pruner or mirror failure feeds the same C13 health state rather than a quieter path. Before 1.3, confirm a snapshot exists and the chip is green **in the profile holding production data**, per the run sheet's backup points.
