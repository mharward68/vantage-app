# AI Context

**Updated:** 2026-08-30 (America/New_York) — **PHASE 1 CLOSE (Prompt 5).** Updated in place over Session 1.8's handoff rather than archived and replaced; 1.8 wrote the phase-boundary document and this amends it. No archive rotation — the next one belongs to Phase 2 Step 0.
**Last run:** Phase 1 / Prompt 5 — goal verification, estimate calibration, standing-file review  **Compartment(s):** documents only, no code touched
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two · `CACHE_NAME` **v83** · `app.js` 13,124 / `index.html` 3,219 / `style.css` 3,148 · deployed n/a
**Estimate vs actual:** Phase 1 planned **8 sessions**, ran **11**. Predicted attention **~105 min**, actual **~88 min**. Sizes were right 10 times out of 11; only 1.8 was wrong. Calibration below.

## Phase 1 is complete — what exists now

- **`state.tasks` and `state.taskSettings`** are first-class state, defaulted through `ensureStateDefaults()`, exported in the ZIP bundle (`prm_tasks.csv`) and restorable from it. Multi-paragraph, comma- and quote-laden notes round-trip character-identical — proved on real data.
- **TaskHub is the sixth hub**, cyan, `#view-tasks` / `nav-tasks`. Six filters (All Open · Past Due · Due Today · Upcoming · Date Range · Completed), sortable headers, 25/50 pagination, overdue/today/done row states.
- **Tasks are edited from two places** — the Prospect Hub inspector's Tasks subsection and TaskHub — through one `openTaskEditor()`. **They are CREATED from one:** the inspector. §15.4 removed TaskHub's "+ New Task" in Session 1.10. The phase goal still says "created and edited from two places" and was never amended; this line is the amendment.
- **Bulk Mark Complete and Edit Due Date** over a page-level selection, with business-day or calendar arithmetic behind a global setting. `shiftTaskDate()` runs in UTC; all three frozen C11 vectors pass.
- **Completion writes prospect history through exactly one writer** (`logTaskCompletionHistory` ← `completeTask`), and `"Task Completed"` is registered in `reachoutTypes` but excluded from the reachout math via `NON_REACHOUT_TYPES` (scope §14 reversed §8).
- **Tier 1 local snapshots** — debounced writer, read-back confirmation, retention (10 rolling + 14 daily + 8 weekly), attachment mirror, staleness watchdog, sidebar health chip. Restored *from* a snapshot on real data, not merely written.
- **Column layout store** (`state.columnLayouts`, C15) with drag-to-resize and drag-to-reorder, persisted through one settings CSV row (C17), verified through a full ZIP round trip.

## What is deferred out of Phase 1

- **`state.taskSettings` is missing from `wipeAllData()`** — found this close, by reading the function. Same miss as `state.tasks` in 1.3, and it means 1.8's drill reported `dateMode` MATCH from a value that was never cleared. Export and restore are genuinely wired (C4 row at `app.js` 2062 / 2665); the *restore* half is unproven. Two lines plus a re-run of that one leg. **Phase 2, data-layer.**
- **`--color-danger` is undefined and has six live call sites**, not the one the earlier note claimed: three delete buttons in `app.js`, two Delete plus Clear Selection in `index.html`, and `renderDomainsView()`'s expired highlight. Five destructive controls across three hubs render without their red. One token definition fixes all six.
- **`parseCSVRow()` (~8770) does not handle escaped `""`.** Not on the restore path; `parseCSV()` is, and is correct.
- **Two Vantage windows silently overwrite each other.** Live defect, not a Phase 4 risk. Rule until fixed: one window at a time. Real fix is the shadow-copy diff in `saveState()`.
- **Carried, needing Michael's eyes:** the 6px resize zone and divider weights (1.8); the 20-row search cap and 220px results max-height (1.9); the `dueDate: 0` code-default-124 assumption and the five default widths (1.10).
- **Still open:** the repo is PUBLIC and DIRECTIVES §0 compliance is undecided — both have Phase 4 treatment in `ai/spec/phase-4-firebase-preflight.md`. The stale `..\backups\` sibling. The stale `E:` clone. `schema_update.sql` is still in the tree and still safe to delete.
- **The AQ inspector drawer has no Tasks subsection.** Deliberate (plan Assumption 2). Phase 2 removes the duplication properly. Do not "fix" it.
- **Six proposed DECLARATIONS amendments are pending Michael's approval**, listed in the Prompt 5 close report — including the DIRECTIVES §0 Firebase/telemetry phase numbers, which still say Phase 2.

## Direction set 2026-08-30 — read this before planning anything

**Vantage is built to be used by one person, not to be sold as-is.** If it is ever commercialised, the enterprise version is a **deliberate rebuild** from the standing documents, not a migration of this codebase — the documents and code go to a fresh planning conversation, which produces the enterprise build plan, and the multi-user problems get solved there with a year of real use behind them. Full reasoning and rejected alternatives: `DECISIONS.md` 2026-08-30, "Commercial path."

Four things follow, and they change how sessions should behave:

1. **Phase 4 is host-for-myself.** Not the foundation of a product. `ai/spec/phase-4-firebase-preflight.md` §1 was written on the opposite assumption and is amended in place — its two "cannot be deferred" items are now ordinary decisions. Owner-scoped security rules are the one part that did **not** soften, and that is Gate A, not Gate B.
2. **Gate B's multi-user clause is relaxed** — stop paying insurance on a migration that will not happen. The rest of Gate B stands, and the single-blob rejection remains correct on its own merits.
3. **The export path is now the most protected thing in the app.** The ZIP/CSV bundle is the bridge to whatever is built next. Keep it whole; re-prove it at every phase close.
4. **No workarounds — build it properly.** Michael's standard. It is the mitigation for the one failure mode of a rebuild-later plan: the code is disposable, the documented reasoning is not, and building properly is how that reasoning gets produced. **This is not licence to abandon compartments** — out-of-compartment work still goes to the backlog. It governs *how* something is built, not *when*. It does convert the two known workarounds-in-place (`taskSettings` in `wipeAllData()`, `--color-danger`) from carried to fixed-in-Phase-2.

**The open question, deliberately unanswered:** whether the sellable version is a genuinely different product or just this one with logins. If the latter, hardening beats rebuilding and the decision above should be revisited. A year of real use is what answers it.

## Estimate calibration — the headline

**Session count ran 37.5% long (8 → 11), and every added session came from one review, of one session (1.5), on one day.** Attention time came in **16% UNDER** (105 → 88 min) — and would have been 43 min without Session 1.8 alone. **The estimates were not wrong about work; they were wrong about which work needs a human.** The one badly-sized session was the QA/close session, sized S because its tasks are small and actually L because verification against real data is where surprises land. Operative numbers for planning the rest: **carry a +35% session-count contingency for any phase that ships new UI, and never size a phase-close session below M.** Phase 2 re-estimates to **7–9 sessions / ~85 min**; Phases 2–4 together to **20–26 sessions / ~270 min**.

## Commit state — corrected

**Session 1.8 is committed and pushed.** Local `main` and `origin/main` are both at `ad7568d` ("Session 1.8: Phase 1 close…"). 1.8's own handoff listed "commit Session 1.8" as outstanding next step 1; that is done and should not be re-run. `ai/APP_BUILD_BOOTSTRAP.md` was modified ~15 minutes after that commit and is the one file likely uncommitted — check `git status`.

## Next step

1. **Commit** whatever `git status` shows outstanding — expect `ai/APP_BUILD_BOOTSTRAP.md` plus this close's document changes.
2. **Take the phase-close backup** — full ZIP export from Data Management, stored outside the project folder, named **`vantage_phase-1-close_2026-08-30.zip`**.
3. **Then Phase 2 — do NOT run Prompt 3.** `ai/spec/prospect-detail-view-scope.md` carries a stop banner: it is approved direction, not a scoped feature, and §5's contract questions are unsettled. **Use `ai/phases/phase-2-RUNSHEET.md`, which starts at Step 0 — Intake.** Prompt 5's scripted closing line says Prompt 3 and is wrong for this phase.

**Carry into Phase 2:** `#canvas-body` is still the shared scroll owner. In `style.css`, check source order before assuming a new rule wins. A drag sharing an element with a click must never re-render that element inside its own mouseup. `--color-danger` and the `wipeAllData()` / `taskSettings` gap are both cheap and both belong to a data-or-CSS session, not to the intake. And close every other Vantage window before starting a session.
