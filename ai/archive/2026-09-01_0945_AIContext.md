# AI Context

**Updated:** 2026-09-01 08:42 (America/New_York)
**Last run:** Phase 2B / **Session 2B.6 — THE CUTOVER.** The ProspectHub prospect card is gone. Every path that used to open it now opens `#view-prospect-detail`. There is one place an existing prospect is edited.
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two (`{'export-backup-btn', 'restore-backup-input'}`), **before and after** · `CACHE_NAME` **v101 → v103, TWO bumps** (v102 shipped the cutover, v103 a comment correction found during verification) — v103 CONFIRMED LIVE on Michael's Chrome, `caches.keys()` `['vantageprm-cache-v103']`, `controlled: true` · `app.js` 14,376 → **14,344** / `index.html` 3,428 → **3,368** / `style.css` **4,271 (UNCHANGED — see Assumption 1)** · **console CLEAN** — 7 lines, zero red · **git state UNKNOWN — this session had no shell**; Michael confirmed in chat that the tree was committed before the session started · deployed n/a
**Estimate vs actual:** sized **L / ~12 min / High**; ran **L**, **two bumps**, confidence held. **Michael's time was ~3 minutes** — two questions answered at the top and nothing else. The Chrome extension was up, so every check ran automated. That is now three sessions in a row confirming 2B.4's lesson: the automation browser being up or down dominates every other sizing factor in this phase.
**One-glance version tell:** click any row in the ProspectHub contacts table. **A full-screen record view opens** — back arrow in the header band, 17 fields, six tabs. v101 and earlier slid a card out on the right. Second tell: `document.getElementById("prospect-inspector")` returns **`null`**.

## What was done

The cutover, in one session as the plan required. **The app is left fully usable** — every prospect surface that existed before this session still reaches a prospect, and the destination is now the finished view instead of a squeezed side panel.

- **FIVE entry points repointed, not four.** The plan's frozen P3 table lists four; the fifth is the **company inspector's "Associated Contacts" roster** (`app.js` ~3739), which also called `selectProspect()`. Left alone it would have set a cursor nothing renders and silently closed the panel — "View Profile" would look broken on a surface in daily use. Michael approved repointing it before the session started. Not a contract change: `openProspectDetail`'s signature is untouched, and 2B.5 already added a caller from the Company tab.
- **`renderInspector()` narrowed to company-only.** The prospect branch and its ~20 `inspector-*` element reads are gone; `hasSelection` is `selectedCompanyId` alone. The `active-row` highlight went with it — a directory row now navigates, so there is nothing left for a "selected prospect" state to describe. The **companies** table keeps its highlight.
- **`#prospect-inspector` removed from `index.html`** — 71 lines. `.prospect-inspector-panel` survives as the **company** host, and `#prospect-inspector-empty` with it.
- **Three functions removed as dead**, each with a comment in its place saying why and warning against restoring it: `selectProspect()`, `deleteProspect()` (a zero-argument wrapper whose whole body read the wrong cursor), and `renderInspectorMemberships()` (its only caller and its only DOM host both went the same session). **The five shared builders 2B.5 extracted stayed** — the detail view's tabs are now their only consumers, which is the one-implementation end-state the phase exists to reach. The SHARED MEMBERSHIP BUILDERS comment block was rewritten because it named `renderInspectorMemberships` as a live consumer.
- **Five listeners unbound**: `btn-edit-prospect`, `btn-delete-prospect`, `btn-edit-inspector-tags`, `btn-add-interaction`, and the `inspector-notes` / `btn-save-pros-notes` pair. **The first two and `btn-add-interaction` used `getElementById(...).addEventListener` with NO optional chaining** — leaving them would have thrown inside `setupEventListeners()` and killed every listener bound after them. That is exactly the failure class `check_ids.py` catches, which is why it ran before and after.
- **The task editor link needed a real fix, not just a redirect.** `saveTaskFromEditor()` ends in `closeTaskEditor()`, which sets `editingTaskId = null` — so reading the id **after** the save (the obvious way to write it) yields `null` every time and the back arrow lands on a bare TaskHub. The id is captured **before** the save. Verified: `originTaskIdMatchesClickedRow: true`.
- **The audience pop-out hides itself before navigating.** `#aud-popout-panel` is `position: fixed; z-index: 99999`. Today's destination is a modal that covers it; the detail view is a `.view-panel` **underneath** it, so left open the panel would sit on top of the record the user just asked to see.

## Verified — real output, not a claim

Ran on **Michael's Chrome via the extension**, against his live `localhost:5000` database, at his real window (`innerWidth` 1269, canvas 989px). Full detail is in the chat; the load-bearing results:

| Check | Result |
| --- | --- |
| Cutover proof | `document.getElementById("prospect-inspector")` → **`null`**. `selectProspect` / `deleteProspect` / `renderInspectorMemberships` all `typeof "undefined"` |
| **EP1** directory row, poisoned from `campaignViewSubState="domains"` | `detailProspectId` `null` → `"pros-jane"`, origin `{view:"prospects", taskId:null, audienceListId:null, campaignSubState:null}`, `activeView "prospect-detail"`, subtitle `"Jane Smith"`, `#nav-prospects` lit, **`active-row` count 0** |
| **EP1** back | `activeView "prospects"`, cursors both `null`, 4 directory rows, panel closed |
| **EP2** task editor link | Clicked row `task-demo-05` → editor open → clicked `#task-prospect-fixed`. `detailProspectId "pros-alex"`, origin `{view:"tasks", taskId:"task-demo-05"}`, **`originTaskIdMatchesClickedRow: true`**, editor closed |
| **EP2** back | `activeView "tasks"`, `#modal-task` **open**, `editingTaskId "task-demo-05"`, title field `"Share the case study"`, prospect `"Alex Rivera — Vercel"` |
| **EP3** audience contact row | `detailProspectId "pros-jane"`, origin carries **all three** campaign keys (`aud-1788023350145` / `"audiences"`), `#modal-prospect` **not** opened |
| **EP3** back, **sub-state poisoned to `"domains"` first** | `campaignViewSubState` back to `"audiences"`, `selectedAudienceListId` restored, audience view visible, `#subtab-audiences` carries `active-filter`, 4 contact rows |
| **EP4** pop-out row | Before: panel `display:flex`, `z-index 99999`. After: **`display:none`, `hidden` true**, same origin as EP3 |
| **EP5** company roster (the fifth site) | `"Stripe"` → `detailProspectId "pros-jane"`, origin `{view:"prospects"}`. **Back arrow returns with the company card STILL OPEN** — `selectedCompanyId "comp-stripe"`, panel `inspector-open`. `selectedCompanyId` is module scope and nothing on the detail path clears it |
| Company card intact | `companyCardHidden: false`, roster 1 row, `activeCompanyRows: 1`. **Screenshot in chat** |
| Campaign container, P9 | `getAttribute("style")` = `"grid-template-columns: 1.2fr 0.9fr; gap: 16px;"` — **byte-identical to the pre-session value**. Computed `528.75px 396.583px`, inspector 396px. **Screenshot in chat** |
| ProspectHub, no panel | `gridTemplateColumns "941.333px 0px"`, panel width 0, **`#canvas-body` scrolls neither way** (`sh 971 = ch 971`, `sw 989 = cw 989`). **Screenshot in chat** |
| `#modal-prospect` still creates | 4 → 5 prospects, new record `pros-1788266169453`, tags `["No Prospect Tag"]`, modal closed, directory refreshed |
| **Two cursors independent** | Detail open on `pros-jane`; drove `openAqInspectorDrawer("pros-alex")`. `state.selectedProspectId` → `"pros-alex"`, **`detailProspectId` unchanged at `"pros-jane"`** |
| AQ drawer opens / edits / deletes | Ran a real query → clicked the result row → drawer **420px wide**, `open`, name `"CUTOVER PROBE2B6"`. Edit opened `#modal-prospect` on `"Jane"`. Delete: 5 → 4, probe gone, drawer closed |
| `check_ids.py` | **Before and after both `{'export-backup-btn', 'restore-backup-input'}`** — the standing baseline pair |
| Console after reload | **7 lines, zero red.** One WARNING is the carried backlog item, see Open items |
| Reload survival, all six hubs | v103 live and controlled; every hub `active-panel` at a real height (903–923px); 4 prospects / 31 tasks / 1 audience(4) / 5 companies / Sarah's 3 tags |
| Cleanup | DB restored from the raw pre-session string, `restoredIdentical: true`, **27,840 bytes before and after**. Zero synthetics, zero probe records |

## Files changed

`app.js` (14,376 → **14,344**), `index.html` (3,428 → **3,368**), `sw.js` (**v101 → v103**), `ai/BUILD_NOTES.md`, `ai/AIContext.md`, `ai/archive/2026-09-01_0842_AIContext.md` (new).

**`style.css` NOT changed** (4,271) — see Assumption 1. **No `DECLARATIONS.md` or `DECISIONS.md` change** — 2B.10 still owes the seventh-view-panel amendment; "six hubs" stays true and must not be edited to say seven.

## Assumptions logged this session

1. **`style.css` needed no edit, though the plan lists it as modified.** Grepped before assuming: **no rule targets `#prospect-inspector` by id.** Every class the removed markup used — `.prospect-inspector-card`, `.inspector-header`, `.inspector-meta`, `.inspector-tags`, `.inspector-quick-actions`, `.inspector-history-section` — still has live consumers in the company inspector, the audience inspector and the AQ drawer. Deleting any of them would have broken three surfaces to tidy one. **Nothing is dead; nothing was removed.**
2. **The fifth entry point was repointed rather than backlogged.** Michael approved it explicitly before the session started. Rationale in "What was done."
3. **A brand-new task departed from replays as `taskId: null`.** `saveTaskFromEditor()` mints the record's id internally and never assigns it back to `editingTaskId`, so there is nothing to capture. `closeProspectDetail()` already handles a null taskId by leaving the editor closed, which is honest behaviour. Recovering the new id would mean reaching into the save path — out of compartment, and worth a backlog line at most.
4. **The pop-out is not re-opened by the back arrow.** The frozen origin record (P1) has no field for it and adding one is a contract change. The user lands on the audience sub-view with the right list selected, which is where the pop-out was launched from.
5. **`selectCompany()` and `closeInspectorPanel()` still clear `state.selectedProspectId`.** Now vestigial, deliberately left. That field feeds the DEFERRED Advanced Query drawer's fallback readers, and touching anything on that path is out of bounds this phase. Harmless: the drawer keys off `aqInspectorRecordId`, and both fallback readers prefer their own explicit target id.
6. **Michael's live database was mutated and restored from a raw string snapshot.** Same forced constraint as 2B.5: `switchView()` calls `saveState()`. The guard was the pre-session `localStorage` string, restored and verified identical at 27,840 bytes, then re-verified by count after a reload.

## Open items

- **⚠️ NEEDS YOUR EYES — THIS IS THE REVIEW POINT, AND IT HAS ALREADY STARTED PRODUCING.** One finding landed within minutes of the close (see Review finding 1 below); the plan predicted exactly this. Phase 1's entire session-count overrun came from one review of the session that first showed the new surface working. **Do a full pass through a real day's outreach in the new view before 2B.7 starts.** The specific things the plan asks you to look at: **the ProspectHub directory at full width with no panel** (screenshot in chat), and whether anything you actually do daily has no home in the new view. Expect this review to produce sessions; they take numbers 2B.11+.
- **⚠️ IF YOU HAD A VANTAGE WINDOW OPEN DURING THIS SESSION, RELOAD IT BEFORE TOUCHING ANYTHING.** Two windows share one `localStorage` and keep independent in-memory state; whichever saves last wins, silently. This session opened its own window, mutated and restored the database, and closed that window again — a second window holding pre-session state would overwrite the restore on its next navigation.
- **✅ REVIEW FINDING 1 — the Company tab's shape is DECIDED. Michael, 2026-09-01, immediately after the 2B.6 close.** This answers open item (a), which has been carried since 2B.5, and it is the phase's **first review-driven session candidate — it takes a 2B.11+ number, not a slot inside 2B.7.**
  **What he asked for:** the company information block is **collapsed by default and expandable**. Opening the Company tab shows the **company name with a disclosure arrow on its left** — his words: *"I should see SPL productions with an arrow on the left indicating I can expand the section."* Clicking it expands the read-only detail grid.
  **What that settles:** the roster leads; the card does not. The 2B.5 question was framed as exactly this fork ("whether the read-only company card earns the vertical space it takes at the top of the roster, or whether the roster should lead and the card collapse") and the answer is the second.
  **The one sub-question the building session must not invent a store for:** whether the expanded/collapsed state persists. **Default to NOT persisting — collapsed on every render of the tab** — which is P4's existing rule for tab and scroll position, and it needs no new field. If Michael wants it remembered, it joins `state.columnLayouts` under its own key, **never a new top-level store**. Confirm with him at build time; do not decide it silently.
  **Scope note:** this is `renderDetailCompany()` and its `.pd-company-card` / `.pd-company-grid` styling only. The roster below it, the three empty states and the colleague-row `openProspectDetail(other.id, detailOrigin)` origin pass-through are all working and verified — do not rework them while you are in there.
- **✅ REVIEW FINDING 2 — "carry me back to the contact I came from." Michael, 2026-09-01. THIS IS TWO ASKS WITH VERY DIFFERENT COSTS, AND A SESSION MUST NOT TREAT THEM AS ONE.**
  **2a — ProspectHub holds the open contact. SMALL, NO ROUTING, and most of it already exists.** His words: *"I want the contact prospect inspector to hold so when I click back to prospect hub from the audience I just went to, it takes me back to the exact contact I just came from."* Verified in the tree, not assumed: **`detailProspectId` is cleared in exactly ONE place — `app.js` 5892, inside `closeProspectDetail()`.** A chip navigation (`switchView("campaigns")` from an Audiences or Campaigns chip) does **not** clear it, so the contact he wants is still sitting in module scope, unused. The sidebar is one line — `switchView(tab.getAttribute("data-view"))` at **`app.js` 12013**. The change is a branch: nav-tab target `"prospects"` **and** `detailProspectId` still set → `switchView("prospect-detail")` instead.
  **The existing design already draws the distinction he wants, for free:** the back arrow is an EXPLICIT exit and clears the cursor (→ ProspectHub shows the directory); a chip is an IMPLICIT leave and preserves it (→ ProspectHub returns to the contact). Do not add a second flag for this; use the clear that is already there.
  **The trap to design against:** with the cursor held, the sidebar can no longer reach the directory in one click — you get the contact, then the back arrow. Confirm that reading with Michael before building; if he wants a one-click directory as well, that is a second control and a different conversation. **Also decide what a nav click to a DIFFERENT hub does** (nothing should change — only the `"prospects"` target is special) and what happens when the held prospect has since been deleted (`renderProspectDetail()` already closes the view and replays the origin on an unresolvable id — reuse that, do not write a second guard).
  **2b — the MOUSE BACK BUTTON. THIS IS A SCOPE AMENDMENT AND MICHAEL SUSPECTED AS MUCH.** It needs browser history entries, which means `history.pushState`, which the scope's Out-of-scope section bans outright: *"Routing of any kind. No `location.hash`, no `history.pushState`…"* — and the `pushState` clause is unqualified, so payload does not save it. **Do not build this inside a Phase 2B session. `CLAUDE.md` requires reading `ai/DECISIONS.md` before proposing to reverse a recorded decision; do that first.**
  **What the analysis found, so a later phase does not re-derive it:** P9's stated concern is narrow and specific — prospect ids are email addresses (`app.js` 2275, 10487), so a hash route puts live contact addresses in the address bar, browser history and any URL-capturing error trace (Gate A + the Phase 4 pre-flight §3.1 telemetry rule). **A SENTINEL design satisfies P9's letter completely:** `history.pushState({vantageDepth: n}, "", location.href)` pushes a history entry **without changing the URL**, carrying only an opaque integer; the real navigation stack stays in memory and `popstate` looks it up. No id reaches the URL, the address bar, the Referer header, or the serialized history state. **So the P9 objection is answerable. The scope ban is the live obstacle, not Gate A.**
  **But the real cost is NOT the `pushState` call, and a session that scopes this by counting lines will underestimate it.** It is that the app would then have **two navigation systems** — the existing `detailOrigin` replay and a browser history stack — which must agree, or the app lies about where the user is (DIRECTIVES rung 2). Specific hazards: the in-memory stack dies on reload while the history entries survive, so back after a reload pops a sentinel with nothing behind it and must degrade to the origin replay rather than doing nothing visible; **forward** becomes an expectation the moment back works; and the modals (`#modal-task`, `#modal-aq-results`, the pop-out) each need a defined answer to "does back close me or leave the view." **Size it as its own session at minimum, and expect the modal matrix to be the part that overruns.**
  **Scope note on both:** he asked for this *"with all of the tabs that have links to other items"* — Audiences chips, Campaigns chips, Company roster rows, Tasks rows. 2a covers all of them at once because it keys off the cursor, not the departure point. That is the argument for doing 2a first and seeing whether it removes most of the need for 2b.
- **Found in the same screenshot, NOT a code bug — a DATA one.** The Headquarters row read **"Arlington, Virginia, Arlington, Virginia"**. `renderDetailCompany()` builds that row as `company.headquarters || company.location || ""` — a **fallback, not a join**, so it renders exactly one field. The duplication is therefore in the stored `headquarters` value itself, almost certainly a CSV import that concatenated city and state onto a field that already held both. **Do not "fix" the render.** The real questions are how many company records carry it and whether the importer still does it — that is import-path work, and it belongs with 2B.7, which is already the phase's one DATA session. Worth one query across `state.companies` before deciding it is a single bad row.
- **⚠️ Carried, unanswered from 2B.4 and 2B.5:** (b) the `.pd-add-block` label "ADD THIS PROSPECT TO AN AUDIENCE" — a full sentence where the old inspector had none. (c) the tab strip's weight against the identity block; whether Interactions is the right default landing tab; the redundant "Tasks" heading inside the Tasks tab; the non-sticky interactions table header.
- **⚠️ THE IDENTITY BLOCK BREAKS CONTRACT S1 BELOW 1150px CANVAS WIDTH — PRE-EXISTING, AND IT IS 2B.11's GROUND.** Unchanged from 2B.5. Not re-measured this session; Michael's 1269px window (989px canvas) is above the breakpoint.
- **⚠️ THE ZERO-BYTE SNAPSHOT IS RECURRING, AND SHARPER THAN 2B.5 RECORDED IT.** This boot logged the identical pair 2B.5 saw — `Ignoring 1 zero-byte snapshot file(s)` then `Pruned 1 file(s) — 1 zero-byte, 0 aged out. 13 kept.` **Same counts, same "13 kept", one session later.** Two readings, and they want different fixes: either a truncated write happens on roughly every session, or **the pruner reports deleting a file it does not actually delete** and has been re-detecting the same one since 2B.5. One `ls` of the snapshots folder separates them. Tier-1 snapshots are the sole protection today, so this is worth the look before Phase 4. **Phase backlog — not this session's compartment.**
- **Found this session, not fixed — to the phase backlog:**
  **`#btn-see-all-contacts` DOES NOT EXIST; the ids are `btn-show-all-contacts` / `btn-show-all-companies`.** Cost one failed tool call. Worth a MAP line.
  **Setting `state.forceShowAllContacts` and `state.forceShowAllCompanies` BOTH true blanks the directory** — the wrapper gets `only-contacts` **and** `only-companies` and CSS hides both halves. The UI never does this (two mutually exclusive buttons), so it is not a user-facing bug, but it looks exactly like a rendering regression from a console.
- **Carried, unchanged:** `#companies-datalist` is dead markup. `pros-sarah` carries no `conference*` keys. The four always-on conference boxes read as grey ghosts. CampaignHub identifies itself twice. Dashboard/DataHub emptiness is `renderDashboardView()`'s `slice(0, 5)`. MediaHub's tag rail off the right edge. `.checkbox-scroller` inline `max-height: 350px`. `.tags-filter-scroller` `max-height: 400px`. `state.taskSettings` missing from `wipeAllData()` → **2B.7**. **`.gitignore` snapshot-glob gap — one line, `vantage_snapshot*`, still NOT fixed.** `favicon.ico` 404 (not seen on this session's boots).
- **2B.11 and 2B.7 queues are unchanged** — the identity redesign (Arrangement A, 2 columns, forced split) and the four new field-slots (`prospects.country`/`postal`, `companies.country`, `prospects.directPhone`).
- **GIT STATE IS UNKNOWN.** This session had **no `device_bash`** — the bridge could stage and commit files but could not execute anything on the machine. Michael confirmed in chat that the tree was committed before 2B.6 started, so the single-session revert path existed; nothing here claims what has been committed since.
- **Unchanged from Phase 1:** two Vantage windows overwrite each other; `parseCSVRow()` `""` gap; repo is PUBLIC; DIRECTIVES §0 compliance undecided; stale `..\backups\`; `schema_update.sql` still deletable.

## Backup coverage — DIRECTIVES §4

**No store of user-writable data was created or modified.** Contract P9 holds exactly: no new field, no `ensureStateDefaults()` entry, no CSV column, no `wipeAllData()` line, no migration. This session **removed** UI, and the one write path it removed — the inspector's notes textarea writing `p.notes` — has a live replacement in the detail view's field 17, committing through `commitProspectField()`. `prospects.notes` already exports, already restores, and is already covered by the ZIP bundle and Tier-1 snapshots. **Covered.**

**The phase-level gate still fires**, through the carried-in `state.taskSettings` gap. **2B.7 must not reason "no data work → Gate C inert."**

Michael's live database WAS opened and mutated this session (one prospect created through `#modal-prospect` and deleted again through the AQ drawer), and was restored byte-identical from a pre-session raw snapshot — see Assumption 6 and the verification table.

## Next step

**REVIEW FIRST. Then Session 2B.7 — email uniqueness, import skip-and-report, and `taskSettings` in `wipeAllData()`.** Compartment DATA with LOGIC, size M.

**⚠️ 2B.7 IS THE PLAN'S FLAGGED ZIP POINT. Take a manual ZIP backup before it starts** — it is the phase's one session that touches write paths and `wipeAllData()`, and its Done-when includes a restore drill.

Do not start 2B.7 before the review pass above. The plan's own words: *"expect that review to produce sessions."*

**Carry forward:** the `🧱 HUB SHELL` block must stay LAST in `style.css`. `#canvas-body` is never edited. `state` is not `window.state`. One Vantage window at a time. `state.selectedProspectId` and `detailProspectId` are two cursors and **do not converge in this phase**. No routing, ever. `switchView()` calls `saveState()` — an in-memory injection persists on the next navigation. Inject the transition kill-switch after every reload and read `document.getAnimations().length === 0` before trusting a measurement. `resize_window` does not work on this machine. **Both query surfaces stay DEFERRED — `renderAqInspectorDrawer()`, the `aq-insp-*` ids, and the Audience Query Engine are not touched, and the AQ drawer being a subsection behind is known and accepted.**
