# AI Context

**Updated:** 2026-08-31 14:43 (America/New_York)
**Last run:** Phase 2A / **Session 2A.3** — Dashboard and DataHub  **Compartment:** UI
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two (`{'export-backup-btn', 'restore-backup-input'}`) · `CACHE_NAME` **v89** · `app.js` 13,249 (untouched) / `index.html` 3,246 / `style.css` 3,432 · console clean, 5 logs 0 errors · deployed n/a
**Estimate vs actual:** sized **M / ~6 min / High**; ran **M**, **one `CACHE_NAME` bump (v88 → v89)**, and roughly **0 min of Michael's time so far** — the whole run was unattended. The review pass is where the time will go, and it has a real question waiting in it (below).
**One-glance version tell:** on the **Dashboard**, the two bottom cards now reach all the way down to the footer instead of hugging their lists. If "Recent Reachout Interactions" is still a short card with white space under it, you are on the old cached document — **reload once more.**

## What was done

**The scroll conversion is complete. All six hubs are on the S5 idiom** and the phase's goal-2 gate is met.

- **`#view-dashboard` and `#view-data-management` both take `height: 100%; min-height: 0`.** Panel gap left at `.view-panel`'s 24px in both — the banner, the stats row and the split grid are separate cards and 24px is already what separates them. Logged, not asked (plan Assumption 8).
- **`.dashboard-split-grid` is written ONCE, unprefixed** (plan task 3). It is shared by exactly these two hubs and nothing else — TaskHub's panel is a `.dashboard-list-card`, not a split grid.
- **`grid-auto-rows: minmax(0, 1fr)` is the line that actually made it work,** and it looks optional. An implicit `auto` row is `minmax(min-content, max-content)`; `align-content: stretch` only hands it *leftover* space, so a row taller than the panel grows past it and the cards never get a bounded box. `grid-auto-rows` rather than `grid-template-rows` because below 900px the grid stacks to one column and TWO rows.
- **The scroll region is deliberately DIFFERENT in the two hubs.** Dashboard's cards are `<h3>` + one feed, so the feed scrolls and the heading stays put (`.hub-scroll` on `#dashboard-recent-reachouts` and `#dashboard-top-media`). DataHub's cards are a heading followed by six loose children with no wrapper, so the **card itself** scrolls and its `<h3>` scrolls with it. The alternatives were inventing a wrapper div inside markup that is almost entirely inline styles, or a sticky `<h3>` over a padded scrollport — which `BUILD_NOTES` already warns does not cover `padding-top`. Both shipped options are one class in `index.html` and no structural change; that is the rung-1 (stability) call.
- **Everything went in the `🧱 HUB SHELL` block at the END of `style.css`.** The block is still last. `#canvas-body` was not touched and still reports `overflow-y: auto`.
- **Measured, not assumed.** Dashboard: panel 989.3px inside a 1037.3px `#canvas-body`; feeds 544px each, scrollHeight 1407 / 1675 with synthetic rows injected → both scroll, `#canvas-body` does not, page does not. DataHub: panel 989.3, cards 832 each, scrollHeight 1080 / 1036 with filler → both scroll, page does not. All six hubs re-measured after a reload: every panel 989.3 (MediaHub 969.3, 20px less because of its own pre-existing horizontal scrollbar), `canvasBodyScrolls: false` in all six.

## Files changed

`style.css` (3,354 → 3,432), `index.html` (3,246 → 3,246 — four class attributes, no new lines), `sw.js` (v88 → **v89**, one bump), `ai/BUILD_NOTES.md`, `ai/AIContext.md`. **`app.js` was not touched.**

## Assumptions — all reversible, all logged

1. **DataHub's card headings scroll away; Dashboard's do not.** Reasoned above. If the DataHub headings leaving bothers you, the fix is a wrapper div per card, not a sticky heading.
2. **Panel gap left at 24px in both hubs**, rather than set to a number.
3. **`.welcome-banner` and `.stats-grid` were pinned with `flex: 0 0 auto`** rather than left at the flex default. Defensive: the default `0 1 auto` plus `min-height: auto` happens to hold them at content height today, but nothing declares that it must.
4. **Both `.welcome-banner` cards were LEFT IN PLACE**, per plan task 4 — 2A.4 removes them, and removing them here would make these screenshots un-comparable with 2A.4's.
5. **Synthetic rows and filler blocks were injected into the DOM** to force both hubs to scroll (5 feed items and a card that fits cannot). `state` was never touched, `saveState()` was never called; a reload confirmed **0 survivors** and identical counts (4 prospects / 5 companies / 30 media / 31 tasks / 0 campaigns / 1 audience list).
6. **`window.prompt` / `alert` / `confirm` were stubbed before switching to DataHub** and restored afterwards. **Zero dialogs were raised** — nothing this session touched goes near `wipeAllData()`.

## Open items

- **⚠️ NEEDS YOUR EYES — and it is the real question the plan flagged, not a checkbox.** Dashboard and DataHub have the least content of the six hubs, and now that they fill the screen it shows. `renderDashboardView()` caps **both** feeds at **5 items** (`.slice(0, 5)`), so with your data the two Dashboard cards are ~750px tall holding one reachout and five media rows — mostly white space. DataHub's right card is the same shape. **Three ways out, and the choice is yours:** (a) leave it — consistent with the other four hubs and 2A.4's header band will tighten the top; (b) raise or remove the `slice(0, 5)` cap so the feeds actually fill what they now own — a one-line `app.js` change, but it is a behaviour change and belongs in its own session; (c) revert these two hubs to page scrolling — a plan revision, which is exactly what the plan said this review is allowed to return. **Screenshots of both hubs are in the chat.**
- **Carried, unchanged:** MediaHub's tag rail is still off the right edge (pre-existing, one-line fix that is not free — still needs your decision, still backlogged with its screenshot). `.checkbox-scroller`'s inline `max-height: 350px` in the AQ Engine. `.tags-filter-scroller`'s `max-height: 400px`. The prospect inspector's squeezed history table (2B replaces it). `state.taskSettings` missing from `wipeAllData()` → **2B.7**. `--color-danger` undefined, six call sites → **2B.3**.
- Unchanged from Phase 1: two Vantage windows overwrite each other (one window at a time); `parseCSVRow()` `""` gap; repo is PUBLIC; DIRECTIVES §0 compliance undecided; stale `..\backups\`; `schema_update.sql` still deletable.
- **Commit state:** local `main` is still **`d282710`** ("Session 2B.2 + 2A.1"); `origin/main` is still **`ad7568d`** ("Session 1.8: Phase 1 close"). **One commit unpushed, and now TWO sessions uncommitted — 2A.2 and 2A.3.** No agent session in this phase has had a shell on the machine; Michael runs git by hand.

## Backup coverage — DIRECTIVES §4

**Not a data session.** No new or modified store of user-writable data; no field, no CSV column, no migration, no `ensureStateDefaults()` or `wipeAllData()` edit; no export or restore function entered. `app.js` was not touched at all. The phase's only mandated backup point before the close was the pre-2A.1 ZIP, already taken.

## Next step

1. **Answer the Dashboard/DataHub question above** — (a), (b) or (c). This is the one input 2A.4 does not need but the plan does.
2. **Answer the MediaHub tag-rail question**, still open from 2A.2.
3. **Commit 2A.2 + 2A.3 and push** — `style.css`, `index.html`, `app.js`, `sw.js`, `ai/BUILD_NOTES.md`, `ai/AIContext.md`, `ai/archive/2026-08-31_1325_AIContext.md`, `ai/archive/2026-08-31_1443_AIContext.md`. The push carries `d282710` with it. Nothing this session added contains a real email, phone or LinkedIn string — the injected rows were named "Synthetic Row N at Placeholder Co" and left the DOM before the reload.
4. **Then Session 2A.4** (header band + one-word names). It depends on all three scroll sessions and they are now all done. It is the phase's flagged overrun risk: six hubs in one edit, twelve screenshots.

**Carry forward:** the `🧱 HUB SHELL` block must stay LAST in `style.css` — that placement is the contract, not decoration. `#canvas-body` is never edited. `state` is not `window.state`. One Vantage window at a time.
