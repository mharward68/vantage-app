# AI Context

**Updated:** 2026-08-31 15:35 (America/New_York)
**Last run:** Phase 2A / **Session 2A.4** — the header band, and the one-word names, **plus its review pass** (sidebar tint, tint tokens, ProspectHub inset)  **Compartment:** UI
**State:** `node --check app.js` clean · `check_ids.py` at its standing baseline of two (`{'export-backup-btn', 'restore-backup-input'}`) · `CACHE_NAME` **v92** · `app.js` 13,272 / `index.html` 3,254 / `style.css` 3,556 · console clean, 5 logs 0 errors · deployed n/a
**Estimate vs actual:** sized **M / ~10 min / High**; ran **M**, **THREE `CACHE_NAME` bumps (v89 → v92)** — v90 the session proper, v91 and v92 the review pass — and roughly **5 min of Michael's time**, all of it in review. Phase 1's "budget two bumps per session and never record a one-bump win before the review" held, and was one short.
**One-glance version tell:** the **sidebar is tinted with the hub's colour**, strongest at the top and fading down, and it changes as you move between hubs. If the sidebar is still flat charcoal, you are on the old cached document — **reload once more.**

## What was done

**Six hubs identify themselves one way now instead of two out of six doing it inconsistently.** Phase goals 3, 4 and 5 are met.

- **`--app-strip-height: 88px` and `--brand-logo-size: 40px` are in `:root` (S6),** and `#sidebar-brand` and `#canvas-header` both read the first one. **Measured: `getBoundingClientRect().height` is 88 on both, `top` is 0 on both** — that is 2A.5's contract check already passing, though 2A.5 still owns the *choice* of number. 88 is what `#sidebar-brand` already measured (40px logo + 24px above and below), so the sidebar is unchanged and the header rose to meet it.
- **The two token values are deliberately literal, not `calc()`-coupled.** Deriving the strip from the logo would have pre-empted half of 2A.5's decision.
- **`#canvas-header`'s inline `min-height` and `padding` at `index.html` 138 are deleted** (S6 requires it — an inline value beats a custom property). Its comment spells both numbers out in words so they do not pollute the grep that proves they are gone.
- **The band tint is `color-mix(in srgb, var(--color-primary) 14% → 4%, transparent)` as a `background-image`,** left to right. `--color-primary-glow` was the obvious alternative and is the wrong one: the per-hub blocks set the glow only in their **dark** rules, so a glow-based band would have carried dark-theme alphas into light mode. `color-mix` against the live token tracks the separate `.light-theme` primary for free, and no colour is named anywhere in the header's rules (S3). Verified resolving: light Dashboard renders `color(srgb 0.231 0.510 0.965 / 0.14)`, dark renders `0.310 0.275 0.898`.
- **The tint is a `background-image`, so the translucent `background-color` and its `.light-theme` override are untouched underneath** — the band is one added declaration, not two themed rules.
- **`#view-title` is 20px (`#brand-name`'s size), `color: var(--color-primary)`, solid — not `#brand-name`'s gradient fill.** A gradient behind a tinted band of the same hue loses its second stop.
- **`#view-icon` is driven from an `icons` map in `switchView()`**, beside `titles`. One map, six entries, the same six emoji the nav uses — the markup carries only the initial `📊`.
- **Both hero cards are gone** (`index.html` 159 Dashboard, 1120 DataHub) and the `.welcome-banner` CSS rules stayed (Assumption 7). DataHub's inline gradient was indigo→cyan on a red hub; it is not mourned.
- **Two `.welcome-banner` selectors were trimmed from the `🧱 HUB SHELL` block's flex rule** — they named markup this session deleted. The base rules survive; only the dead view-prefixed selectors went. The 2A.3 comment describing both hubs as "a `.welcome-banner` above a split grid" was corrected in the same edit.

## Then the review pass — three more changes, all Michael's calls

- **The sidebar carries the hub colour too.** `#sidebar` gets the same `color-mix` derivation as the band, ramped **vertically** (180deg) rather than horizontally, so the sidebar's top-left corner and the band's left edge both land on the strong stop and meet at matching strength across the seam. Fading downward also leaves the red Wipe Data / Reset Sandbox controls and the snapshot chip on almost no tint. Vertical for a second reason: the 280px↔76px collapse cannot stretch or reposition a vertical ramp.
- **The two tint stops are now tokens** — `--hub-tint-strong: 14%` and `--hub-tint-soft: 4%` in `:root`. The header and the sidebar both read them, so **the open tint-strength question is now ONE edit of two numbers, not four literals.** That was the point of factoring them.
- **ProspectHub's "Export CSV" was reported falling out of the window. It was not — it was FLUSH**, and the A/B cleared both this session and 2A.2: with every 2A.2 and 2A.4 rule force-disabled the button's right edge sat at 1245 against a card ending at 1245 (**0px**); with them on, 1244 (**1px**). Pre-existing, and the phase had made it 1px better. Cause: `.prospects-directory-card` has `padding: 0`, and the search/filter row above supplies its own `padding: 20px` — so that row looked inset while everything below ran edge to edge. Fixed with `padding-inline: 20px` on `#view-prospects .prospect-directory-tables`, **20px taken from the header row rather than invented**, so the headings now align under the search box and the buttons under the buttons. Gap is now **21px** and `headingAlignsWithSearch: true`.

## Files changed

`index.html` (3,246 → 3,254), `style.css` (3,432 → 3,556), `app.js` (13,249 → 13,272), `sw.js` (v89 → **v92**, three bumps), `ai/BUILD_NOTES.md`, `ai/AIContext.md`. **`#canvas-body` was not touched and still reports `overflow-y: auto`.**

## Assumptions — all reversible, all logged

1. **`subtitles.dashboard` carries BOTH halves of the old hero card's copy** — the greeting and the sentence — as one line: *"Welcome back to Vantage PRM — accelerating your media reach, outreach campaigns, and prospect pipelines from one unified desk."* The plan's task 6 says "Dashboard's sentence" (the `<p>`); Assumption 4 quotes the `<h2>`. Keeping both loses nothing and matches the descriptive shape MediaHub and DataHub already use. **Trimming it to just the sentence is a one-string edit.**
2. **`--app-strip-height` starts at 88px**, the value that leaves the sidebar pixel-unchanged. 2A.5 may move it.
3. **The band tint is 14% → 4%.** This is the number the plan sends to Michael's eyes.
4. **Native dialogs were NOT stubbed before DataHub.** The extension's content filter refused the stub script twice. Instead the render path was read first — `renderDataManagementView()`, `renderSnapshotStatusPanel()` and `updateBackupFolderUI()` contain no `alert`/`confirm`/`prompt` call — and **nothing in DataHub was clicked.** Zero dialogs appeared.
5. **The pin toggle was un-set by removing `.sidebar-pinned` directly, not by clicking the checkbox,** for the collapsed screenshot, and re-added afterwards. **CORRECTED at the review pass — an earlier version of this line said "that class is DOM-only; nothing persists it", and that is wrong.** The state DOES persist: `setupEventListeners()` reads `localStorage['vantage_sidebar_pinned']` at boot and adds or removes the class from it. Manipulating the class directly (what this session did) changes nothing stored, because only the checkbox's `change` handler writes the key — and setting `.checked` from script does not fire `change`. So the method used was safe, but for a different reason than the one first written down, and **a session that clicks that toggle to take a screenshot changes a real user preference.**
6. **No synthetic rows were injected this session** — every hub had enough real content to judge the band. `syntheticSurvivors: 0` after two reloads.
7. **The sidebar tint reuses the band's stops rather than getting its own pair.** If the sidebar wants to be softer than the header, it needs a third token; today they are deliberately the same two numbers.
8. **The ProspectHub fix went in ProspectHub's existing rule inside the `🧱 HUB SHELL` block**, not a new rule elsewhere in `style.css` — the block is last in the file, so it cannot lose on source order, and ProspectHub's rules stay together.

## Open items

- **⚠️ NEEDS YOUR EYES — three, in order of how much they matter.**
  **(a) The tint strength, in both themes — now across BOTH the band and the sidebar, so it matters more than it did.** `--hub-tint-strong: 14%` / `--hub-tint-soft: 4%`, one edit in `:root`. Dashboard's blue in light theme is still the faintest of the six and reads as almost nothing; ProspectHub's purple and DataHub's red are the strongest. **Watch the active nav tab if you raise it** — it marks itself with `--color-primary-glow` and now sits on a background of the same hue.
  **(b) CampaignHub now identifies itself TWICE.** Its panel still opens with an in-page "Outreach Campaign Manager" heading and subtitle, directly under a band that already says CampaignHub. It is not a `.welcome-banner`, so the plan's task 6 did not reach it — but it is the same inconsistency goal 4 exists to end. **This is the contingency session 2A.7 if you want it.**
  **(c) The Dashboard/DataHub emptiness question from 2A.3 is still unanswered and this session made it more visible** — removing the hero card gave both feeds ~90px more height to be empty in. The `slice(0, 5)` cap is still the cause, not the layout.
- **Carried, unchanged:** MediaHub's tag rail off the right edge (pre-existing, one-line fix that is not free). `.checkbox-scroller`'s inline `max-height: 350px`. `.tags-filter-scroller`'s `max-height: 400px`. The prospect inspector's squeezed history table (2B replaces it). `state.taskSettings` missing from `wipeAllData()` → **2B.7**. `--color-danger` undefined, six call sites → **2B.3**.
- Unchanged from Phase 1: two Vantage windows overwrite each other (one window at a time); `parseCSVRow()` `""` gap; repo is PUBLIC; DIRECTIVES §0 compliance undecided; stale `..\backups\`; `schema_update.sql` still deletable.
- **Commit state:** local `main` is still **`d282710`** ("Session 2B.2 + 2A.1"); `origin/main` is still **`ad7568d`**. **One commit unpushed and THREE sessions uncommitted — 2A.2, 2A.3 and 2A.4 (including its review pass).** No agent session in this phase has had a shell on the machine; Michael runs git by hand. **This is the largest carried risk in the phase**: `git checkout` is no longer a per-session fallback, because reverting one file takes three sessions' work with it.

## Backup coverage — DIRECTIVES §4

**Not a data session.** No new or modified store of user-writable data; no field, no CSV column, no migration, no `ensureStateDefaults()` or `wipeAllData()` edit; no export or restore function entered. The only `app.js` change is the `titles` / `icons` / `subtitles` maps inside `switchView()`. Record counts identical before and after, across two reloads: 4 prospects / 5 companies / 30 media / 31 tasks / 0 campaigns / 1 audience list.

## Next step

1. **Answer (a), (b) and (c) above.** Only (a) is a 2A.4 fix; (b) is 2A.7 if you want it; (c) is still 2A.3's.
2. **Commit 2A.2 + 2A.3 + 2A.4 and push.** Do this before 2A.5 — three uncommitted sessions is a bad state to take a fourth edit onto.
3. **Then Session 2A.5** (logo size and strip alignment). It needs **one input from you at the top of the session: the logo size**, chosen looking at the app. Binding constraint: the sidebar collapses to 76px, so ~56px is safe and past roughly 64px it gets tight. Note the S2 equality check already passes at 88/40 — 2A.5 is choosing a better-looking pair, not repairing a broken one.

**Carry forward:** the `🧱 HUB SHELL` block must stay LAST in `style.css`. `#canvas-body` is never edited. `state` is not `window.state`. One Vantage window at a time. **The `titles` map's keys are view ids — "data-management" is DISPLAYED as "DataHub" and identified everywhere as `data-management`.**
